# T-071 Strategic detail policy

Luna, 2026-09-08. Source/tests/report frozen for final aggregate. Root inspected `artifacts/T-071-crowd-no-effects.png` and accepted the strategic display correction. The final rendered 1000-machine fixture still fails real-time capacity.

Empty placement circles are now shown only when their actual projected adjacent-slot spacing is at least12px and the projected ring band is at least16px. At smaller spacing, occupied markers plus the selected and pointer-targeted slots remain. The selected wedge outline is unchanged. Building labels require enough projected spacing for the measured font width plus12px, and a40px band, or are retained for the exact selected/hovered slot. Exact selected information remains in the existing status. GUI-consumed pointer motion clears the world hover. Both base/live and experimental art preview use this policy. No geometry, click targets, transactions, actors, cap, clock or gameplay rules changed.

Focused checks: strategic11, build49, art59, hotkeys420 =539 passing. Actual input checks prove a suppressed slot remains purchasable, occupied/selected/hover affordances survive, GUI clears hover, close zoom restores detail, and broken wedges retaining occupants produce no invalid slot geometry. Tracked hidden bounded Godot PIDs11768/35768/18940/37008 all exited; no script errors. Root-certificate warning remains as before.

## Final rendered evidence

Exactly one fresh effects-off and one fresh effects-on run after this change, using the same T067/T068 helper, 12rings/all1000 durable mixed actors, speed1, unchanged weapon cadence, 1440x810 GL/VSync1, three wall-seconds warmup and120 measured frames. Reproduction: `godot --path . --script tests/performance/measure_application.gd`, adding `-- --effects-on` for the second mode. Internal deadline45s/process55s; both completed without deadline or gameplay errors. Hardware/fixture details and pre-change results remain in [T-067.md](T-067.md).

| Median / p90, milliseconds | Effects off | Effects on |
|---|---:|---:|
| Whole frame |198.582 /213.390|247.318 /265.468|
| Simulation callback |177.127 /191.891|220.513 /237.268|
| Sync |7.761 /8.128|7.569 /7.841|
| Draw submission |6.158 /6.368|6.067 /6.260|

Each window advanced16 simulated seconds, over23.82966 wall seconds off and29.875244 on: simulation/wall ratios0.6714 and0.5356. Median8/max9 ticks per rendered frame. All1000 actors remained; effects-on reached39 concurrent traces. The draw cost is lower than the retained pre-change21.54/21.58ms medians, but whole frames remain far above16.667ms and simulation does not keep up with wall time. Callback measurement includes pre-hit snapshot work when effects are enabled; drawing is CPU submission, not GPU completion. No further optimization loop or capacity claim.

Final PIDs40852(off) and28536(on), both exited. Standard median averages sorted59/60; p90 nearest-rank index107. Raw frame samples and component statistics are in `artifacts/T-071-measure-off.json` and `T-071-measure-on.json`, with original stdout `.txt` alongside. Actual final PNGs are `T-071-crowd-no-effects.png` and `T-071-crowd-effects.png`. The earlier T067 captures/results and incomplete first effects-on diagnostic remain preserved as pre-change evidence.
