# T-047 — Playable growing world

Luna, 2026-09-07. **Ready for review: the main live scene buys and operates through ring 12 using actual input.** New growing-world suite: 58/0. Existing grid inspector: 6668/0; build 49/0; live 85/0; weapon catalogue 73/0; Structure 13/0; wall 84/0; Tunneler 47/0. Rendered growing-world fixture: 59/0. Both final 1280×900 images were opened and inspected.

## Implementation

`live_view.gd` exports `unbounded_expansion=false`; `scenes/live_view.tscn` enables it. Unbounded simulation creation receives 9223372036854775807, the largest positive signed 64-bit integer. This is an internal representational bound, not a chosen game cap. No grid is allocated at that size. The initial `ring_limit` stays three; displayed geometry synchronizes to `max(initial extent, historical owned count + 1)` only when the extent changes. Twelve owned rings therefore have thirteen displayed bands and 157 cells including the core.

The grid rebuild has an explicit preservation path: extending geometry retains camera position/zoom/orientation, selection and dragging. Normal purchasing still clears its completed build mode exactly as before. The synchronized grid drives world conversion, selection, slot placement and wall edges. Marker conversion starts with the displayed extent and also includes any farther live actors/destinations. HUD focus on ring 12 now points into actual selectable geometry. A new run deliberately restores the original centered three-band view, selection, walls and marker support.

Existing bounded test fixture helpers explicitly set `unbounded_expansion=false` before creating their three-ring simulations. Their existing limits remain meaningful. The standalone build and grid-inspection scenes retain their bounded behavior. No gameplay/core/profile files changed.

## Actual-input evidence

The fixture instantiates the real main scene and checks its enabled flag and unchanged 160 starting energy. It then disables normal spawning and grants 1,000,000 energy solely for the test. Every ring purchase from 2 through 12 uses the existing Ring Plate button and a real world-slot click; camera position/zoom, resulting selection, cell counts and unchanged-extent grid identity are asserted after each purchase.

Actual HUD input focuses ring 12/wedge 4, where actual buttons/clicks build a Flak and wall and repair a damaged wedge. A fixture tombstones ring 11, then actual Reclaim input restores its Relay while ring 12 survives. A real pool actor at ring 13/wedge 4/radial fraction 0.8 (HP 1000, DPS 0, speed 1) advances one real simulation tick and is checked against its marker position, then released. Pan, zoom and middle-button release over the panel are exercised. Setting fixture funds to zero rejects ring 13 without state/geometry growth. Actual Menu/Retry resets the expanded run without stale geometry or extra funds.

The capture reuses those same checks. [Twelve-ring overview](artifacts/T-047-twelve-rings.png) shows the owned world plus next preview; [outer-ring focus](artifacts/T-047-outer-ring-focus.png) shows the restored inner Relay, outer Flak/wall and selected ring 12. Capture-only camera positions/zoom are center/0.28 and HUD focus/0.8. Funds display zero because the capture retains the unaffordable-purchase fixture. Normal gameplay gets no funding grant, speed change or extra starting rings.

## Process evidence and limits

Pinned `E:/Godot/Godot_v4.7.2-stable_win64.exe` reported `4.7.2.stable.official.ed1daf0bf`. All launches used hidden tracked process objects, 30-second timeouts and process-local `.godot/appdata` / `.godot/localappdata`. Logs: `.godot/t047-*.out` / `.err`. Initial PID33688 could not compile the newly added gameplay `AbilityRules` because its global class cache was stale; no gameplay fix was made. Bounded import PID12616 registered it, then valid growing-world run PID35892 passed. Further PIDs: grid23160, build38008, live13000, catalogue31796, Structure26228, wall41132, Tunneler31388, capture26984. Every process exited before timeout. Only the environment's existing root-certificate-store startup warning remains; final runs contain no script/parse/assertion errors.

The existing fixed-size text/slot marker treatment is intentionally preserved. At overview zoom, Relay labels in this deliberately aligned fixture can overlap; the focused capture remains readable. This task does not redesign density or adopt art. It validates operation through twelve rings, not unlimited world-size performance or final capacity. Geometry remains proportional to displayed rings, and slot rendering grows with their slot counts. No aggregate suite or new crowd benchmark was run in this lane; integration review owns those decisions.
