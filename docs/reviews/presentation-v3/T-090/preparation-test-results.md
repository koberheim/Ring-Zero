# T-090 preparation verification

Terra Stream E, 2026-09-09. **2690/2690 logical checks pass**, exit0, Godot4.7.2.stable.official.ed1daf0bf, headless. Source/test files were then frozen and the sole Godot slot returned to B. This is a pure API preparation result; no native readability, art, performance or human gate is passed by it.

| Check | Result |
|---|---|
| Hysteresis and physical transform size projection | PASS; full outer scale, tilt, rotation, shear and all twelve occupied-axis bearings |
| Living visible actor conservation | PASS;1000 standards plus six visible elite classes, no cap/discard; dead, burrowing and offscreen excluded |
| Density counts, bounded batch coverage and pan-stable bearings | PASS; ties and empty field do not manufacture a dominant direction |
| Reduced-motion and matching atlas frame regions | PASS logical mapping; native animation/material map proof pending |
| Fresh HP/relay/brownout/collapse projection | PASS; own wedge max HP, repair/reconnection and immutable prior snapshot |
| Radar physical band floor and exact click/focus identity | PASS logical geometry at1/6/12 rings and0.75/1/1.15/1.3/1.5 outer scale, six painted pixels after separator |
| Existing full-suite regression | Pending B/root integration run; no full-suite claim from this isolated check |
| Current native art/800-1000 stress/historical1031 fixture | Pending integration lease and native capture |
| Timed human recognition/readability | Pending human answers |

The first run, `preparation-01`, failed to compile `FortressStatus.relay_active` because an inferred Dictionary expression had no fixed type. Godot still printed a misleading partial1487-check count after the dependent call failed; the wrapper correctly rejected the run on script-error detection despite exit0. The entire command/stdout/stderr/engine log is retained. An explicit `bool` declaration repairs the cause. `preparation-02` completes2690 checks with no script errors; the existing Windows root-certificate-store warning remains unrelated to these logical projections.

Reproduce after coordinating the sole Godot lease:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File docs/reviews/presentation-v3/T-090/run-preparation.ps1 -Label reviewer-01
```

Use a new label to retain owner evidence. The wrapper uses isolated APPDATA/LOCALAPPDATA, starts a hidden headless process, retains the native exit handle, enforces30s timeout, and rejects parse/script errors independently of exit code. Exact arguments/timestamps are in the `.command.json` artifacts. Headless execution does not exercise RTX4080/NVIDIA610.88 rendering; no GPU performance number is implied.
