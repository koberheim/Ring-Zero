# T-092 v2 provisional industrial elite delivery

Six mechanism-led 3D models, separate relightable maps and eight mechanical poses per class are ready for E's integration. The user explicitly authorized continued provisional production while human review remains pending; this is recorded in the current contract. **T-092 is not Done and no human/native gate is claimed passed.**

Review [industrial models](industrial-models.png), [material channels](material-channels.png), [mechanical playback](motion-review.html), and the new [16-physical-pixel silhouette trial](public/index.html). [Handoff](HANDOFF.md) describes exact runtime paths/conventions. The [palette](palette-material-diagnostic.png) and [twelve-bearing](twelve-bearings-diagnostic.png) sheets are clearly labeled offline diagnostics, not gameplay evidence. Material-sheet thumbnails enlarge small atlas frames for inspection; source renders and motion strips remain native 256 px per frame.

The source is `assets/art/source/presentation-v3-elites/v2/t092_industrial_elites_v2r1.blend`, with reproducible `scripts/art/t092_production_blender.py`. It contains an isolated `T092_Production_v2` scene, `T092V2_Industrial_Elites` collection, six class collections, 356 objects including cameras/lights/rigs and 10 private materials. Models have real beveled casting edges, raised fasteners and plates, cut drill teeth, booster bells/pistons, an anchored moving press jaw, telescoping radial probes, an offset ram, and accreted broken-ring hull fragments with moving feed clamps. No detailed textures were derived from old AI albedo. The current user file was inspected before use and remained Untitled with its original active Scene/Cube/Light/Camera. Existing user materials were untouched; new private materials remain in the session. Blender lease was released after the repaired source export.

`renders/` retains 192 corrected native256x256 images: six classes x eight poses x albedo/normal/emission/beauty. Cycles used CPU rendering, eight samples for maps, sixteen plus denoising for beauty. All maps use transparent background. The beauty-only key/fill/rim setup is a review light rig; those lights are absent from albedo/normal/emission outputs. No current `.blend` save or overwrite occurred: isolated library writes created the new versioned sources. The initial v2 library remains alongside v2r1 for history; use v2r1.

The CPU processor `scripts/art/t092_production_review.py` exports 24 atlases into `assets/art/machines/presentation_v3_v2/`, validates them, creates native motion strips/GIF playback, creates a separately shuffled v2 human trial, and hashes source/output/evidence. It does not launch Godot/Blender or alter existing shipping textures. Export paths are ready to consume but no runtime view references them yet.

| Check | Result | Evidence / limit |
|---|---|---|
| Distinct approved dominant forms and real depth | Prepared; author and Astra visual review | `industrial-models.png`; this is not a blind human pass |
| 48 aligned animated map frames; 24 atlases | PASS artifact integrity | `artifact-checks.json` |
| Visible mechanical changes | PASS artifact integrity | 8 distinct Tunneler pixel frames, 5 per symmetric in/out mechanism; native strips/GIFs |
| Exact alpha alignment and clear frame margins | PASS artifact integrity | All albedo/normal/emission alpha identical; no clipping at frame border |
| True geometric normal convention/calibration | PASS artifact integrity | Roof RGB127,171,247; opaque vector-length quantization error <0.0063; all opaque normals viewer-positive |
| Transparent edge RGB/coverage handling | PASS artifact integrity | Four-texel RGB extension, alpha unchanged, no black albedo RGB immediately outside coverage |
| Human >=11/12 monochrome identification, confusion check | PENDING | New v2 public trial and separate private key; no answers supplied |
| Native 20-degree all-bearing seating, palettes, mixed crowd, physical floors | PENDING E integration | Offline diagnostics do not substitute |
| Native mechanical event/heading/reduced-motion behavior | PENDING E integration | Source loop moves local parts only; final event adapter absent |
| 42+ suites, import/export, performance, T-079 closure | PENDING integrated check | No Godot process or shared view edits in this lane |

Two implementation defects were caught before integration. First, Blender's isolated scene needed explicit scene/view-layer context and dependency updates; without them all eight output frames had identical pixels despite different PNG metadata. The motion check failed and stopped export. `failed-motion-initial/` retains frame00/frame04 for every class plus all initial beauty hashes and the failure record. The render recipe now overrides the isolated scene/view layer, updates the layer before each pose/map, and was rerun for all192 images. Pixel uniqueness then passed. Second, a strict edge check caught black RGB from negative-lobe resampling of premultiplied color/coverage. Exact2x area averaging replaced that reducer, and edge checks now pass on all48 frames. No failed maps were wired into the game.

Reproduce in a fresh isolated Blender namespace: inspect/claim the instance; load the production script definitions and call `build()` once. The script refuses an existing v2 scene. In each later MCP invocation reload definitions, then call `render_frames(kind,0,8)` for each kind. Existing render files are skipped unless `force=True` is explicitly used for a repair. Call `save_source('a-new-versioned-source-name.blend')`; it refuses existing paths. Read back preserved user context and release the lease. New source creation uses `bpy.data.libraries.write` on the private scene and dependencies, not a save-main-file operator. Retained source can be appended as a scene from the library. Then run:

`python scripts/art/t092_production_review.py`

The final command passed all artifact assertions. `manifest-sha256.json` covers source libraries, raw renders, exported atlases/metadata, review artifacts and both scripts; its own file is excluded. A final independent hash readback is recorded in the delivery message. Machine date at delivery is 2026-09-09, America/New_York. Production started from `ff1de6d`; metadata records HEAD at export, while manifest hashes identify this uncommitted asset delivery. Renderer is Blender5.2.1/CyclesCPU. GPU/driver and game backend are not performance evidence here; no timing claim is made. Resolution is native256 maps before128/256 atlas framing, camera20 degrees, alpha-monochrome trial16 physical px, animation phases0..7, game simulation tick not applicable, actor count0.

Changed paths: two named production scripts; `assets/art/source/presentation-v3-elites/v2/`; new `assets/art/machines/presentation_v3_v2/`; this v2 review directory; `../v2-first-inspection.png`. Original v1 package/source and all existing shipping assets remain untouched. No shared runtime source, planning log or commit was written by this lane. E owns integration and its native acceptance slot. Retain the human trial as pending until an actual reviewer supplies answers.
