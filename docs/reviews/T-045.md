# T-045 — Phase 8 weapon controls and functional feedback

Luna, 2026-09-07. **Ready for Astra review: 351 focused functional checks and 12 rendered-capture checks pass.** Both final 1280×900 PNGs were opened and inspected. Validation followed Terra's T-043 freeze, including the D-094 occupied broken-wedge repair rule.

## Scope

The existing Weapons tab now offers Flak, Mass Driver, EMP Node, Lance Emitter and Point Defense. Prices come from the active profile. All five use existing mode selection, placement, cancellation, pause and Retry. World labels use the approved catalogue names, including the previously unlabeled Armor Plating and Repair Node. Existing marker geometry, font, colors and layout are reused; no art direction is selected. The five weapon rows fit without scrolling. Rendering labels after all board fills fixes the existing painter-order issue exposed by the longer Point Defense label; later wedge geometry no longer erases part of its name.

Power-capacity errors now describe power for every weapon. D-094's occupied broken-wedge repair error has specific feedback. In Repair/Reclaim mode the existing status line shows the authoritative live-service quote for the hovered world target, or the existing selection before hovering. Hover performs no purchase. The same left click still purchases immediately; there is no new confirmation or second-click policy. Reclaim's slot preview follows the existing restored-slot mapping. No selected-ring power dashboard was added.

## Fixtures and evidence

The new `test_weapon_catalogue_view.gd` uses actual Viewport input for each new button, placement, occupied/broken/energy/power rejection, right cancellation over controls, menu and Retry. It checks one-time energy charges, unchanged rejected state and no ghost occupants. Existing Flak/Mass Driver power feedback is covered. Structure checks compare displayed repair/reclaim costs with authoritative quotes before spending, then verify the same charge on purchase and the D-094 rejection.

All generic fixtures disable normal spawning and grant 10,000 test energy. The power-rejection fixture overrides `power.base_output=10`, exhausted by the unchanged starter Flak. Combat fixtures override only starter Flak damage to zero in addition to disabled spawning/funding. They insert durable normal actors through the real pool API with HP 1000, DPS 0 and speed 1. Broken interior approach wedges are fixture state, allowing valid interior normal actors. Purchased EMP must damage/stun its own-ring actor while excluding another ring; purchased Lance must hit outward and exclude inner-ring and adjacent-wedge decoys.

Point Defense uses the existing scheduled Tunneler fixture: normal spawn rate zero, first Tunneler arrival 0.05 seconds, unchanged two-second burrow, Flak damage zero, wedge base HP 100,000, test energy 1000 and a purchased second ring. The fixture clears the starter Flak slot and purchases Point Defense there through actual input. Underground immunity and damage after authentic emergence are checked; no target phase or successful event is fabricated.

The capture fixture disables normal spawning, sets starter Flak damage zero, grants 10,000 energy and buys ring two. Actual input places the three new weapons in ring-one wedges 2/3/4, then Armor Plating and Repair Node in ring-two wedges 2/4. A ring-one wedge is set to 40 HP solely to show the authoritative repair preview. Final PNGs: [weapon catalogue](artifacts/T-045-weapon-catalogue.png) and [structure labels / repair quote](artifacts/T-045-structure-quote.png), both 1280×900. Combat is evidenced by the actual simulation tests described above, not a fabricated visual event.

## Validation and process record

| Suite | Passed | Failed |
|---|---:|---:|
| New weapon catalogue view | 73 | 0 |
| Existing build view | 49 | 0 |
| Existing live view | 85 | 0 |
| Existing Structure view | 13 | 0 |
| Existing wall view | 84 | 0 |
| Existing Tunneler view | 47 | 0 |
| Rendered capture fixture | 12 | 0 |

The new suite initially had three cost-format assertions comparing an explicitly converted float against the profile's original integer display; assertions now compare the exact profile-derived button text. The existing build suite's bottom-corner background click landed on Menu after the catalogue grew. Its background-interception check now clicks the panel's top border; its original atomicity/selection assertions remain. These were fixture corrections, not relaxed production checks. Final build/catalogue suites were rerun after the drawing-order correction. All other focused suites passed before that drawing-only correction; the final capture verifies the resulting board, labels and controls.

Pinned engine: `E:/Godot/Godot_v4.7.2-stable_win64.exe`, reported `4.7.2.stable.official.ed1daf0bf`. Each launch used process-local APPDATA `.godot/appdata` and LOCALAPPDATA `.godot/localappdata`, `Start-Process -WindowStyle Hidden -PassThru`, an exact process object, a 30-second timeout and workspace stdout/stderr logs. Headless command: `--headless --path . --script res://tests/presentation/test_<suite>_view.gd`; capture command: `--path . --script res://tests/presentation/capture_weapon_catalogue_view.gd --resolution 1280x900`.

Tracked PIDs: initial catalogue 38028; catalogue 39432; initial build 6944; live 41052; Structure 11284; wall 36576; Tunneler 30144; corrected build 40160; initial capture 32204; final catalogue 34984; final build 35628; final capture 43292. All exited within timeout; no child remains. Logs are `.godot/t045-*.out` and `.godot/t045-*.err`. The environment's existing root-certificate-store warning appears on startup; no final script/parse/assertion errors occurred.

## Limits

Final copy review removed decision IDs and tier labels from both Repair/Reclaim player prompts. The correction occurred after Sol's 21:15:40 UTC aggregate run. The catalogue suite passed again (73/0, PID 33204), and both PNGs were recaptured (12/0, PID 3904); the updated Structure PNG was opened and inspected. Both processes exited within their 30-second limits. No other mode prompt contains a decision ID or tier label. Final source/capture freeze is after this correction.

This integration does not complete Phase 8: Terrain and abilities remain separate rule/UI work. It does not resolve A2 growing-world integration, choose art, change tuning, or make a new performance/capacity claim. Existing accepted HUD density and performance scope remain intact.
