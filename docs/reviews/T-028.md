# T-028 Tunneler presentation review

Luna delivery, **2026-09-07**, after T-027 gameplay acceptance and approved D-077 / D-079 Option A. **Visible lifecycle is ready for review; angular load performance remains unresolved.** Focused Tunneler checks: **47 passed**. Existing live/wall suites: **66 / 84 passed**. Final rendered lifecycle suite: **36 passed**, with five actual 1280x900 PNGs opened and inspected. No functional or capture assertion fails remain.

## Implementation

`live_view.gd` now creates `LiveSimulation.create(profile, ring_limit, true)`. There is no player-facing feature toggle, timing override or grant. Main scene, normal startup, data, controls, slots and menu/clock policy are unchanged.

The view reads independent runtime snapshots. Burrowing Tunnelers contribute a hollow 3-screen-pixel destination circle and a `Tunneler N.N s` countdown, with the existing 14-pixel font offset 12 screen pixels right and 12 up. Warning geometry is drawn after board/building geometry and never intercepts world input. Remaining time derives only from the supplied burrow duration/elapsed fields on simulated ticks; the view neither advances underground state nor chooses a destination. No normal circle appears at the moving underground position. The active counter includes underground actors.

Surface-attack and roaming phases use a separate shared triangle MultiMesh at current snapshot positions, keeping the approved 3-screen-pixel radius. Normal actors remain filled circles in their existing MultiMesh. There are no per-actor scene nodes, marker deduplication or simulation culling. All derived render points are disposable presentation caches, not authoritative Cartesian state. Real disappearance from the snapshot removes the marker.

**Known visibility limit:** the approved small surfaced triangle can coincide with the starter Flak's larger disk. It remains visible but is difficult to distinguish inside that disk. This is explicitly retained in the surfaced-overlap capture; no unauthorized size, color, offset or styling change hides the issue. The destination countdown is readable beside the same occupied point at both captured zoom levels. A separate scheduled three-ring fixture shows the triangle at an empty destination for shape inspection.

Files changed in this lane: `src/presentation/live_view.gd`, new `tests/presentation/test_tunneler_view.gd` / `capture_tunneler_view.gd`, and the existing `capture_wall_view.gd` fixture wrapper so comparable normal-load measurements enable the feature flag and report phase counts. No gameplay/core/profile source changes were made.

## Functional and rendered evidence

Actual scheduled admission drives every Tunneler lifecycle fixture; phase, targetable and event arrays are never fabricated. Actual Viewport input and button signals cover pause/Resume, right cancellation over UI, pan/zoom, middle release over controls, Flak placement during a warning and a newly purchased Mass Driver killing the surfaced actor. Assertions cover locked warning position while the underground actor moves, countdown frozen with pause, no underground wedge damage, real emergence, warning-to-triangle replacement without duplicate circles, roaming updates, kill reward exactly once, inside attack/collapse clearing inner buildings while preserving outer Relay, warning persistence when another actor collapses its destination, and no continued ticks after runtime failure/core loss.

The main-view flag is verified by an actual default-timing scheduled arrival: the test writes a temporary selected profile under `.godot/t028-quiet-profile.json` with normal spawns disabled, Flak damage 0 and wedge HP 100000, **preserving `tunneler.first_arrival_seconds = 60`**. It constructs the view through its normal `_ready`, supplies fixture purchase funds for ring 2, verifies no Tunneler before 60 seconds and an admitted warning at 60 seconds. This temporary test profile is not the normal player startup profile.

All captures use real rendering after `RenderingServer.frame_post_draw`, not headless placeholders:

- [Underground warning](artifacts/T-028-underground-warning.png): two-ring accelerated scheduled fixture, remaining countdown shown as 1.7 s at the locked ring-1 / wedge-12 midpoint, coincident with the starter Flak. There is no marker at the underground moving position.
- [Warning with zoom](artifacts/T-028-warning-zoom.png): same paused-in-test simulation snapshot after two real wheel-up inputs. Font and warning stay fixed in screen size and the countdown remains readable beside the Flak.
- [Surfaced overlap](artifacts/T-028-surfaced-overlap.png): the same two-ring fixture advances to real surface_attack, preserving the visibility limitation where the 3-pixel triangle lies inside the starter disk. This is intentionally retained as review evidence.
- [Unobscured surfaced triangle](artifacts/T-028-surfaced-triangle.png): a separately declared three-ring scheduled fixture emerges at ring 2 / wedge 12 midpoint, between ordinary slot marks and inside the outer perimeter. Real MultiMesh transform checks confirm current position and 3-pixel screen radius under zoom; warning and normal-circle duplicates are absent.
- [Tunneler-caused inner collapse](artifacts/T-028-inner-collapse.png): the scheduled Tunneler breaks the seventh inner wedge from inside, triggering actual collapse. Inner buildings disappear; ring 2 and its Relay survive; the roaming triangle remains visible.

### Exact accelerated fixture settings

The shared lifecycle fixture starts from the testing profile with `pressure.spawn_per_second = 0`, `tunneler.first_arrival_seconds = 0.05`, `flak.damage = 0`, and `health.wedge_base_hp = 100000`. All other tuning is unchanged, including `tunneler.burrow_seconds = 2`, `tunneler.arrival_interval_seconds = 30`, `health.tunneler_hp = 20`, and `standard_machine.damage_per_second = 5`. A test-only 1000-energy state grant buys ring 2 via the simulation API with Relay wedge 3 / slot 1, leaving 760 energy. The warning is captured after 22 actual 1/60-second ticks.

The unobscured surface capture additionally buys ring 3 through the simulation API with Relay wedge 3 / slot 2, leaving 400 energy, then waits for the actual scheduled emergence. The collapse fixture instead pre-breaks ring-1 wedges 1–6 and sets wedge 12 HP to 0.01 before scheduled arrival. The Tunneler's real inside attack supplies the seventh break; no fake collapse event is injected. The separate locked-warning test lets a normal machine cause collapse during burrow through a declared pre-broken ring-2 / wedge-12 approach gap. These state mutations exist only in tests/captures. **Normal first arrival remains 60 seconds, not 0.05 seconds.**

## Commands and tracked processes

All runs used pinned `E:/Godot/Godot_v4.7.2-stable_win64.exe`, cwd `E:/AI Projects/Games/Ring Zero`, process-local APPDATA `.godot/appdata` and LOCALAPPDATA `.godot/localappdata`, hidden tracked launches and local logs. Functional/capture waits were bounded at 20 seconds. Measurements used 35-second process waits plus the unchanged internal 25-second deadline. Every process exited; no child remains running.

```powershell
$env:APPDATA = Join-Path $PWD '.godot/appdata'
$env:LOCALAPPDATA = Join-Path $PWD '.godot/localappdata'
$godotTask = Start-Process -FilePath 'E:/Godot/Godot_v4.7.2-stable_win64.exe' -ArgumentList '<arguments below>' -WindowStyle Hidden -PassThru
Write-Output "Godot PID $($godotTask.Id)"
if (-not $godotTask.WaitForExit(20000)) {
    Stop-Process -Id $godotTask.Id
    throw 'Godot task timed out'
}
```

| Run | Arguments after executable | PID | Result |
| --- | --- | ---: | --- |
| Tunneler lifecycle | `--headless --path "E:/AI Projects/Games/Ring Zero" --script tests/presentation/test_tunneler_view.gd --log-file .godot/t028-tests.log` | 24424 | **47 passed, 0 failed**, exit 0 |
| Existing live | Same headless/path, `--script tests/presentation/test_live_view.gd --log-file .godot/t028-live-tests.log` | 21400 | **66 passed, 0 failed**, exit 0 |
| Existing wall | Same headless/path, `--script tests/presentation/test_wall_view.gd --log-file .godot/t028-wall-tests.log` | 30800 | **84 passed, 0 failed**, exit 0 |
| Final five captures | `--path "E:/AI Projects/Games/Ring Zero" --script tests/presentation/capture_tunneler_view.gd --log-file .godot/t028-captures.log` | 34256 | **36 passed, 0 failed**, exit 0 |
| Sealed 1000 | Same rendered/path, `--script tests/presentation/capture_wall_view.gd --log-file .godot/t028-load-sealed.log -- --sealed` | 14416 | **1029 passed, 0 failed**, exit 0 |
| Angular 1000 | Same rendered/path/script, `--log-file .godot/t028-load-angular.log -- --angular` | 392 | **1030 passed, 1 strict angular failure**, exit 1 |
| Exactly one requested fresh-process angular repeat | Same rendered/path/script, `--log-file .godot/t028-load-angular-repeat.log -- --angular` | 39420 | **1030 passed, 1 strict angular failure**, exit 1 |

Initial capture PID 35548 exited on a test-script inferred-type parse error; explicit Vector2 typing corrected it. PID 37672 then passed 28 capture checks; PID 10748 passed 32 after the unobscured fixture was added; the final run above adds retained overlap evidence. No process timed out. Passing runtime logs contain only the environment's existing root-certificate-store diagnostic. Measurement failures are explicit continuity assertions, not script errors or core loss.

## Comparable enabled-feature load measurements

The normal 1000-machine fixtures are unchanged in population, geometry, HP 1e8, DPS 1, speed 1, 11 Flak, three-second warmup, 120 frames and 60 Hz clock cadence; their simulation construction now passes `true`. Sealed contact uses 12 ring-1 walls and 1000 machines distributed over all 12 outer-surface wedge centers. Angular routing uses fully purchased ring 3 with wall gap 12 and machines at radius 384 in wedges 5–7. No Tunneler is due before 60 seconds, and all final phase counts confirm **1000 normal circles, zero underground warnings, zero surfaced triangles**. No mixed 999+1 benchmark was substituted.

All measurements use OpenGL 3.3 Compatibility on RTX 4080 / driver 610.88, VSync mode 1 enabled, max_fps 0. Median is the mean of the two central samples; p90 is nearest-rank. Per-frame simulation timing sums actual simulation.step calls, sync includes snapshot/render-cache/UI refresh, and draw is CPU draw submission. Frame intervals also include uninstrumented engine/render/VSync/scheduling overhead; no isolated GPU attribution is claimed.

| Metric (median / p90) | Sealed | Angular first diagnostic | Angular fresh repeat |
| --- | ---: | ---: | ---: |
| Frame interval ms | **16.828 / 19.300** | **101.119 / 116.579** | **103.576 / 117.637** |
| Simulation CPU ms/frame | 10.430 / 12.942 | 93.880 / 109.262 | 96.286 / 110.494 |
| Snapshot/refresh CPU ms/frame | 4.090 / 4.154 | 4.153 / 4.285 | 4.146 / 4.238 |
| Draw CPU ms/frame | 0.958 / 0.980 | 1.509 / 1.556 | 1.511 / 1.554 |
| Ticks/frame | 1 / 1 | 6 / 7 | 6 / 7 |
| Total ticks | 127 | 578 | 579 |
| Simulated interval seconds | 2.117 | 9.633 | 9.650 |
| Route rebuilds during sampling | 0 | 0 | 0 |
| Active min / max | 1000 / 1000 | 1000 / 1000 | 1000 / 1000 |
| Angular probe frames | 0 / 120 | **66 / 120** | **65 / 120** |
| Machines with changed angles | 0 | 1000 | 1000 |
| Weapon hits | 440 | 0 | 0 |
| Walls / wedges broken | 0 / 0 | 0 / 0 | 0 / 0 |
| Wedge HP delta | 0 | -1447.256229 | -1480.589531 |
| Wall HP delta | -529.166162 | 0 | 0 |

The previous T-025 view measurement was sealed 16.650 / 16.863 ms and angular 49.120 / 49.666 ms with 120/120 angular frames. **The current angular result regressed, and both fresh processes fail the unchanged strict condition.** Catchup lengthened the sample until machines reached the opening and began wedge contact, so these are mixed-motion diagnostics rather than accepted full-window angular evidence. All 1000 instances and actual movement were retained; no criteria, cap, speed, cadence, fixture placement or gameplay implementation was changed to force a pass. Exactly one repeat was requested and performed; measurements then stopped.

Simulation remains the largest measured cost, with six to seven ticks per angular frame, while sync is about 4.15 ms and draw about 1.51 ms. These measurements do not isolate the cause of the regression between runtime revisions and enabled-feature presentation overhead; they are returned as an unresolved bottleneck. The sealed result is near a 60 Hz interval but its p90 exceeds 16.667 ms. Neither result establishes final 1000-cap performance.

## Review limits

The five captures and functional checks support Kevin's visible Tunneler-stage review after Astra validation. The tiny triangle/building overlap and angular performance regression are explicit limitations. There is no new final art, balance, audio, power, ability, or FPS acceptance claim. Source/tests/captures/report are stable, all Godot processes have exited, and the exclusive measurement window is released.
