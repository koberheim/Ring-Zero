# T-024 â€” bounded motion reconstruction experiment

Owner: Sol. Date: 2026-09-06. Outcome: **no safe measured gain; production restored to accepted T-020 logic**. No gameplay, cadence, validation, or numerical-policy change is retained. The exclusive Godot window has been released to Astra.

The proposed specialization preserved the unchanged angular coordinates during radial motion and unchanged radial coordinates during angular motion. Boundary-adjacent inputs fell back to the original full reconstruction, preserving its snapping, legal-cell ownership, and failure behavior. Both endpoints still underwent all original public validation and radius representability checks. Two bounded variants were tried: one combined guarded reconstruction function, then one split-helper dispatch. Neither demonstrated a reliable overall improvement, so both were removed. No wider optimization or system diagnosis was pursued.

## Correctness evidence

Historical artifact `docs/reviews/artifacts/T-024-motion-comparison.gd` contains the frozen accepted T-020 helper and the comparison instrumentation used during this experiment. The routine `tests/core/test_polar_motion.gd` suite was restored by reversing only the T-024 additions; it does not retain a duplicate implementation. Existing T-020 checks remain. An additional 1,975 direct before/after comparisons cover radial/angular partial motion, inward/outward and clockwise/counterclockwise directions, all wedges, seams, arbitrary and boundary-adjacent fractions, ordinary/large rings, zero and elapsed target arrival, endpoint tolerances, diagonal rejection, overflow/underflow, invalid speed/delta, null input, and unrepresentable progress/band inputs. Comparisons require identical success/failure, diagnostic strings, reached flag, and consumed time; successful results must have exactly the same ring/wedge ownership, independent output objects, radius difference at most `1e-8` world units, and bearing difference at most `1e-12` turns. Ten repeated split-travel steps per axis additionally check timing and positions against the prior helper. Prior tests cover half-turn ties and input isolation.

Baseline, both candidates, and final restored production all printed `polar_motion: PASS`, exited 0, and passed every comparison. Candidate numerical differences were confined to retaining an unchanged fraction rather than recalculating it within the established tolerance. Since no specialization is retained, final production has no numerical output or failure-semantics change.

## Method and measurements

Each axis fixture supplies the same 1,000 legal partial segments to frozen and current helpers: rings 2â€“8, all 12 wedges, radial fractions `0.2 + (index % 7)/10`, angular fractions `0.123 + (index % 5)*0.13`, speed 96 world units/second, delta `1/60`. Radial targets are two rings outward at the same fractions; angular targets are one clockwise wedge onward at the same fractions/radius. Inputs are built outside timing. The fixture runs five warmups and twenty measured 1,000-call batches per implementation per axis, alternating execution order. Each call uses the complete public API and adds returned ring to a checksum. Median averages the middle two sorted samples; p90 is nearest rank. Times below are milliseconds for 1,000 calls.

| Run | Axis | Frozen median / p90 | Current median / p90 |
|---|---|---:|---:|
| Baseline: identical logic | Radial | 4.3670 / 4.427 | 4.3535 / 4.448 |
| Baseline: identical logic | Angular | 4.3370 / 4.459 | 4.3120 / 4.363 |
| Combined guarded candidate | Radial | 4.4170 / 4.757 | 4.5085 / 4.793 |
| Combined guarded candidate | Angular | 4.3900 / 4.451 | 4.3995 / 4.546 |
| Split-helper candidate | Radial | 4.3355 / 4.738 | 4.4350 / 4.776 |
| Split-helper candidate | Angular | 4.3655 / 4.560 | 4.3305 / 4.408 |
| Restored: identical logic | Radial | 4.4790 / 4.556 | 4.3755 / 4.436 |
| Restored: identical logic | Angular | 4.4470 / 4.543 | 4.2975 / 4.465 |

The split candidate's angular median difference was approximately 0.8%, while its radial median regressed approximately 2.3%. Even identical logic differed by up to approximately 3.4% in the restored comparison. These observations do not establish a safe improvement. The combined candidate likewise showed none. A possible branch/property-access overhead explanation is only a hypothesis; no attribution is claimed.

Headless helper timing includes public validation, float guards, position allocation, result dictionaries/error arrays, caller loop/branch overhead, and checksum access. Neither candidate removed per-call output allocation. No allocation profiler was run. This is not a full simulation or rendered measurement, and no frame rate, crowd reduction, or cadence change is inferred. Frozen nested-class versus production global-class execution is also an instrumentation difference; identical-code controls expose some resulting/background variability without identifying its cause.

## Execution and scope

Pinned Godot 4.7.2 stable official `ed1daf0bf`, workspace-local APPDATA/LOCALAPPDATA. All runs used hidden tracked processes with a 20,000 ms timeout; none timed out. Astra explicitly released the exclusive measurement window before the first run. Production was not edited while Terra's diagnostic was running.

| Run | PID | Exit | Wrapper elapsed |
|---|---:|---:|---:|
| Baseline | 6024 | 0 | 0.845 s |
| Combined candidate | 30008 | 0 | 0.943 s |
| Split candidate | 3184 | 0 | 0.946 s |
| Restored production | 12696 | 0 | 0.952 s |

Executed from `E:\AI Projects\Games\Ring Zero`; identical command shape for each log (`t024-baseline.log`, `t024-specialized.log`, `t024-split.log`, `t024-restored.log`):

```powershell
$env:APPDATA = Join-Path (Get-Location) '.godot/appdata'
$env:LOCALAPPDATA = Join-Path (Get-Location) '.godot/localappdata'
$motionProcess = Start-Process -FilePath 'E:/Godot/Godot_v4.7.2-stable_win64.exe' -ArgumentList '--headless --path . --script tests/core/test_polar_motion.gd --log-file .godot/t024-restored.log' -WindowStyle Hidden -PassThru
Write-Output "Test PID=$($motionProcess.Id) timeout=20000ms"
if (-not $motionProcess.WaitForExit(20000)) { $motionProcess.Kill(); throw 'T-024 tests timed out' }
$motionProcess.Refresh()
Write-Output "Exit=$($motionProcess.ExitCode)"
Get-Content .godot/t024-restored.log
```

The existing certificate-store startup diagnostic appeared; no script/assertion failures occurred. No gameplay consumers or rendered trial were run by Sol. Retained delivery is the historical comparison/measurement artifact and this report, with production and the routine test suite restored. Stop here for Astra review as the brief requires when specialization produces no safe gain.

## Restoration verification and retained artifact

Production source was compared with the frozen original after removing only the nested test-class wrapper/one indentation level, restoring the original `class_name PolarMotion` declaration, and normalizing the final newline. Case-sensitive full-source equality returned **true**. SHA-256 of both reconstructed frozen source and production file: `B33A9E70DFD3B06D7F5FD4988518823E57EDCCB4DADE163439B82F13E1A87FBD`.

Routine tests were restored by reversing the exact T-024 additions (comparison/benchmark members and initialization calls, then the appended instrumentation/frozen class). The retained suite is 98 lines with no frozen/comparison/benchmark code; SHA-256 `C48CB4260C9F34716C1F0B68F93BE683D40788895F24538B5796A9EA6F502205`. No pre-edit independent test-file hash was captured, so this is a deterministic inverse-edit restoration rather than a claimed comparison against an independently saved test hash.

The complete script that passed the final PID 12696 run is preserved as `docs/reviews/artifacts/T-024-motion-comparison.gd`, SHA-256 `02BF0CD9F0186C15509B426C80CFD09704412DC4E65FB630232EDBF16F9D5D6B`. The final run log is `.godot/t024-restored.log`; at execution time the artifact occupied the routine test path shown in the commands above. It can be invoked explicitly with `--script docs/reviews/artifacts/T-024-motion-comparison.gd` for historical investigation. No Godot run followed relocation: Luna owns the measurement window. The restored routine test contents were already exercised as part of that final passing run.
