# T-068 root acceptance notes

Feature implementation is accepted for the private test candidate. This is not acceptance of final balance, art, large-swarm real-time capacity or every historical Phase13 human evaluation criterion.

Root reviewed gameplay/profile/recording contracts and source, the Transfer/Assembler and numerical movement corrections, immutable/atomic boundaries, final UI behavior and native screenshots. Final39-suite native aggregate passes with no timeout or nonzero child exit. All246 stored gameplay digests remain unchanged. No baseline regeneration was used to hide a change.

The exported main application passed Start, real Flak placement, pause/resume, abandonment, Quit and independent reload using messages sent only to its verified owned window. Root independently read its trace/profile: accepted purchase tick78, pause/resume tick138, abandoned end154; zero reward, next runID2 and no pending run. Both processes exited0 through the UI. Global foreground input was unavailable; two guarded attempts sent no input. The unsupported external-script attempt is recorded separately. Physical desktop routing remains a manual follow-up.

Final build identity b1df72c682cb1405d76422f67998cfdbffc2e858662c6a1b120e0507bd4f8fb3 matches all221 inventoried source files; root independently ran the verifier after package smoke. Original specification and Kevin's real profile were not edited by this effort. Final fonts match their recorded SHA256 hashes and include OFL notices.

Root accepted minimum/130% results, upgrade details, letterboxed Start, fullscreen settings, HUD/relay failure and final strategic crowd screenshots. Empty placement markers and building text now reduce at unreadable spacing while occupied/selected/hovered affordances remain; input geometry is unchanged. The UI follows the authorized industrial direction; world art remains open.

Large-crowd performance is the main engineering follow-up. The final12-ring/1000-actor rendered fixtures retain all actors and have no gameplay errors, but median frames are198.582ms effects-off and247.318ms effects-on, with simulation/wall ratios0.6714 and0.5356. No60FPS or final-capacity claim. Full raw evidence and limits are in T-068-performance.md and T-071.md.

The completed user briefing is feature-completion-briefing-2026-09-08.md. D111-D119 are logged for later confirmation, not new permission blockers. Source snapshot/inventory follows documentation freeze; final handoff identifies its exact path. No publication or external distribution occurred.