# T-026 delivery — pure Tunneler foundation

Implemented the approved testing profile and pure schedule/destination descriptor. No live simulation, core, weapon, presentation, or normal spawn behavior changed. Source and tests are frozen for Astra acceptance; exclusive Godot window is closed.

Changed files: `src/gameplay/tunneler_rules.gd`, `src/gameplay/balance_profile.gd`, `data/balance/testing.json`, `tests/gameplay/test_tunneler_rules.gd`, `tests/gameplay/test_balance_profile.gd`, `docs/balance-data.md`, and this report. Godot generated UIDs for the new scripts.

`arrivals_between` validates the profile and finite nonnegative interval, counts `(start,end]` with the specified 1e-12 arrival-unit tolerance, and rejects signed-count overflow without partial results. Exact time zero always precedes a positive first arrival, including values smaller than the tolerance. `spawn_descriptor` validates state/profile/sequence, computes scheduled-time growth, finds the outermost and next inward intact wedges on the repeating 12,1…11 bearing, and skips missing-inner cases. Its 14-field descriptor owns independent position/start/destination objects and records an immutable-by-input-change destination. No backlog or runtime state is created.

Validation used Godot 4.7.2, workspace `.godot/appdata` and `.godot/localappdata`, hidden `Start-Process -PassThru -WindowStyle Hidden` processes with finite `WaitForExit` (20 seconds for import/consumers; 15 for Tunneler). All completed normally; no process termination was needed.

| Check | PID | Result | Log |
| --- | --- | --- | --- |
| Editor import | 6184 | exit 0 | `.godot/t-026-import.log` |
| Tunneler rules | 24448 | 224 checks, 0 failures, exit 0 | `.godot/t-026-tunneler-tests.log` |
| Balance profile | 35760 | 432 checks, 0 failures, exit 0 | `.godot/t-026-balance_profile.log` |
| Building consumer | 24284 | 45 checks, 0 failures, exit 0 | `.godot/t-026-building_rules.log` |
| Weapon consumer | 17888 | 342 checks, 0 failures, exit 0 | `.godot/t-026-weapon_rules.log` |
| Normal spawn consumer | 33276 | 147 checks, 0 failures, exit 0 | `.godot/t-026-machine_spawn_rules.log` |

Reproduce from the project directory using `E:/Godot/Godot_v4.7.2-stable_win64.exe` with `--headless --path . --editor --import --quit` for import, or `--headless --path . --script tests/gameplay/test_<suite>.gd --log-file .godot/t-026-<suite>.log` for each listed suite (`tunneler_rules`, `balance_profile`, `building_rules`, `weapon_rules`, `machine_spawn_rules`). Use the tracked hidden process and finite wait described above. The recurring Windows root-certificate-store diagnostic appears in each test log; no script failures occurred.

Coverage includes exact arrival boundaries, interval partitions, bounded tolerance, tiny positive first arrivals, malformed profiles/intervals/state/sequences, time/count/HP overflow, all bearings across two cycles, two/three-ring destination selection, broken and collapsed gaps, skipped bearings without deferral, descriptor reference isolation and locked destinations, default additive stats and editable overrides. Strict profile checks cover missing/extra/type/range errors for every new field. Prior consumer checks were retained.

Runtime admission, pool cap handling, burrow progression, emergence vulnerability, subsequent routing, and warning appearance remain for a separate handoff. No performance or rendered claim is made for this sparse pure helper; no crowd benchmark or digest baseline was changed.
