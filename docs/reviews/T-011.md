# T-011 build view delivery

Implemented by Luna for Astra review, 2026-09-06. The startup scene now exposes neutral build controls for the accepted testing state: owned ring 1, Flak at wedge 12 / slot 0, Relay at wedge 6 / slot 0, and 20 energy.

Changes are confined to `src/presentation/build_view.gd`, `scenes/build_view.tscn`, `tests/presentation/test_build_view.gd`, this report, capture PNGs and generated adjacent UID/import metadata. In `project.godot`, only `run/main_scene` changed. Existing grid inspector and its suite are intact.

The inspector subclass consumes BalanceProfile, BuildingRules and RingPurchaseRules. Expansion prices and next-ring slot counts come from the quote API, including eligible but unaffordable quotes. Owned slot counts come from state. Farther unowned rings show boundaries only. Occupants use the authoritative slot_position API; alternating label offsets separate neighboring occupied slots. No placement coordinates are persisted.

World actions use unhandled input so Control events do not also select or purchase. The inherited early input is overridden; middle-button release and right-click cancellation are handled there, ensuring both work over a panel. No world selection or purchase runs in early input. Purchase failures retain the selected build mode and preserve state. Tab changes, right cancellation and menu actions never spend. The always-processing menu pauses the tree and restores its prior paused state on closing.

## Verification

All commands ran from `E:/AI Projects/Games/Ring Zero` with pinned `E:/Godot/Godot_v4.7.2-stable_win64.exe`. Process-local environment:

```powershell
$env:APPDATA = Join-Path $PWD '.godot/appdata'
$env:LOCALAPPDATA = Join-Path $PWD '.godot/localappdata'
```

- Import: `--headless --path . --editor --import --quit --log-file .godot/t011-import.log` — exit 0; final import includes scripts and PNGs.
- Focused headless: `--headless --path . --script tests/presentation/test_build_view.gd --log-file .godot/t011-tests.log` — final review correction: 49 passed, 0 failed, including right-click cancellation over a panel without spending or changing selection.
- Preserved inspector: `--headless --path . --script tests/presentation/test_grid_inspection.gd --log-file .godot/t011-inspector.log` — 6,668 passed, 0 failed; run once.
- Rendered focused suite: `Start-Process -FilePath 'E:/Godot/Godot_v4.7.2-stable_win64.exe' -ArgumentList '--path "E:/AI Projects/Games/Ring Zero" --script tests/presentation/test_build_view.gd --log-file .godot/t011-capture.log -- --capture' -WindowStyle Hidden -PassThru` — exit 0; 60 passed, 0 failed (48 functional checks plus 12 capture checks). OpenGL Compatibility on RTX 4080. The final rendered run includes the widened panel so all three tabs are visible.

Actual Viewport.push_input mouse events cover selection, Flak placement, occupied/unowned/core/unaffordable attempts, right cancel, panel and button click-through protection, menu/Resume, paused placement, pan/zoom, cross-panel drag release, full ring 2 purchase and broken inward wedge rejection. Button signals also exercise Mass Driver and mode entry. Other checks cover disabled Terrain, unsupported mode rejection, quoted unaffordable prices, no passive income, preexisting pause restoration and missing-profile safe startup. Test-only funds exercise expansion and Mass Driver; no player-facing grant exists.

Final review added right-click-over-panel cancellation in early input and its actual-input regression. Only the focused headless suite was repeated for that correction, as requested by Astra. There were no visual changes; the 60-check rendered result and four captures above precede this input-only correction.

Initial focused execution found two inferred Variant type parse errors; both were fixed with explicit bool types before successful runs. The stalled initial process was stopped. Godot consistently emits `Failed to read the root certificate store` in this environment; it does not prevent import, tests or rendering. No other runtime errors remain in passing logs. Integration revealed no rule changes, so unchanged rule/data suites were not repeated.

## Rendered evidence

All four actual viewport PNGs are 1280x900, captured after `RenderingServer.frame_post_draw`, opened and visually inspected after the final run:

- [Startup](artifacts/T-011-startup.png): energy 20, owned ring 1, starter Flak/Relay, empty slot marks, Weapons/Structure/disabled Terrain.
- [Placed weapon](artifacts/T-011-placed-weapon.png): selected ring 1 / wedge 3 / slot 0, new Flak, energy 0.
- [Ring 2 purchase](artifacts/T-011-ring2-purchase.png): 12 new wedges, Relay at wedge 3 / slot 1, energy 260 after the 500-energy fixture buys the 240-energy ring; next quote 360.
- [Menu](artifacts/T-011-menu.png): paused panel with Resume; purchase controls disabled.

This is an intermediate build view. No spawning, enemy movement or combat loop, passive income, walls, power, solar abilities or manual firing controls are included. Neutral labels are inspection placeholders; no human usability or completed live-combat claim is made.
