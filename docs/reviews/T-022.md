# T-022 wall view review

**Latest measurement: post-T-025.** The unchanged rendered fixtures now both pass, including **120/120 continuous-angular frames and all 1000 machines**. Sealed frame interval is **16.650 ms median / 16.863 ms p90**; angular is **49.120 / 49.666 ms**. Full-cap angular performance remains below a 60 Hz budget. The appended T-025 section supersedes earlier angular-continuity failure conclusions while retaining them as historical evidence.

Luna delivery, 2026-09-06, with final post-T-023/T-024 rerun. **Correctness/presentation is ready for review; full-cap rendered performance remains unresolved.** Final focused results are **84 wall, 66 live and 49 standalone build checks passed, zero failed**. Four actual 1280x900 captures pass **51 rendered checks** and were opened and visually inspected. Main scene remains `live_view.tscn`; this presentation lane did not edit gameplay/core/balance files.

## Final corrected-fixture rerun

Astra accepted three T-023 simulation corrections; T-024 axis specialization showed no reliable gain and the original PolarMotion was restored. Source was stable for this exclusive measurement window. Both fixtures now use the approved test-only HP **1e8** / DPS **1**, retain all 1000 machines, speed 1, 11 Flak, a three-second warmup, 120 frames, unchanged VSync and clock cadence. No criteria were relaxed. The representation change permits measurable wall damage while remaining durable throughout both tests.

| Final measurement (median / nearest-rank p90) | Sealed contact | Mixed angular/contact diagnostic |
| --- | ---: | ---: |
| Full frame interval ms | **18.986 / 30.840** | **96.529 / 148.919** |
| Simulation CPU ms/frame | **12.659 / 24.561** | **89.475 / 141.865** |
| Snapshot/refresh CPU ms/frame | 3.766 / 3.878 | 3.849 / 3.962 |
| Draw submission CPU ms/frame | 0.958 / 0.991 | 1.512 / 1.564 |
| Ticks/frame | 1 / 2 | 6.5 / 8 |
| Total measured ticks | 172 | 628 |
| Measured simulation seconds | 2.867 | 10.467 |
| Route rebuilds during sample | 0 | 0 |
| Active min/max | 1000 / 1000 | 1000 / 1000 |
| Angular probe frames | 0 / 120 | **57 / 120** |
| Machines changing angle across sample | 0 | 1000 |
| Weapon hit events | 605 | 0 |
| Walls/wedges broken | 0 / 0 | 0 / 0 |
| Wedge HP delta | 0 | -2247.255466 |
| Wall HP delta | -716.665983 | 0 |

The sealed fixture completes with measurable quarter-DPS wall damage and unchanged wedge HP, but its frame interval remains above a 16.667 ms budget. The angular diagnostic completes all 120 frames yet **fails the unchanged continuous-angular assertion**: machines detour, then reach the opening and damage its exposed wedge. Only 57 of 120 representative frame samples remain angular, despite all 1000 changing angles during the overall interval. It is not valid full-window funnel performance evidence. Simulation CPU remains dominant, with multiple catchup ticks per rendered frame and zero measured route rebuilds. These results support presenting the concrete wall behavior to Kevin with explicit limitations, **not accepting final 1000-machine-cap FPS**.

All final children completed within their timeouts, no benchmark criteria or source were altered during this rerun, and the lane is frozen:

| Final rerun | PID | Log | Result |
| --- | ---: | --- | --- |
| Rendered sealed | 24724 | `.godot/t022-load-sealed-corrected.log` | 1027 passed, 0 failed; exit 0 |
| Rendered angular | 29608 | `.godot/t022-load-angular-corrected.log` | 1028 passed, 1 continuous-angular failure; exit 1 |
| Wall suite | 33756 | `.godot/t022-wall-corrected-tests.log` | **84 passed, 0 failed**; exit 0 |
| Live suite | 34792 | `.godot/t022-live-corrected-tests.log` | **66 passed, 0 failed**; exit 0 |
| Build suite | 37984 | `.godot/t022-build-corrected-tests.log` | **49 passed, 0 failed**; exit 0 |

Commands use the same tracked hidden-launch pattern below with the corrected log names. No captures were rewritten during this measurement-only rerun; the four reviewed wall-behavior images remain current presentation evidence.

## Source behavior

The live Structure tab now offers Wall with the profile-driven 5-energy price. Actual wedge clicks call `LiveSimulation.place_wall`; no view calls WallRules directly. Wall mode ignores building-slot occupancy, spends once on success and clears; duplicate, insufficient, unowned and broken-support failures retain mode without mutation. Wall is a live-only mode: small virtual mode/prompt/placement hooks preserve standalone build support and behavior. Live idle copy covers both wedges and slots.

Active walls draw only their supporting wedge's curved outer arc in the existing neutral white/gray treatment, 3 screen pixels wide. Geometry uses the established bearing/ring-bound APIs and is cached as derived presentation data. No side edges are introduced as walls. Selected active walls append their current HP to the existing selection label. Broken support hides its wall line; collapse removes inner lines and leaves surviving outer walls and Relay intact. Wall-break feedback uses the existing label, below core-loss, collapse and wedge-break priority.

The existing all-target MultiMesh rendering, continuous polar-angle conversion, 3-screen-pixel machine radius, camera controls and 60 Hz clock are unchanged. Pause, GUI isolation, panel right cancellation and middle release remain effective. Core loss and runtime errors disable the Wall button and block its placement.

The old inner-collapse helper now pre-breaks ring 2 / wedge 7 before inserting the attacker at radius 192. This gives the machine a legal approach gap under T-021 and preserves the other outer wedges/Relay. `capture_live_view.gd` inherits that corrected helper; historical T-018 PNGs were not regenerated. The live panel-background test click moved to the feedback area because the new Wall row expands the panel, making its former bottom-edge point overlap Menu.

## Verification and process evidence

All runs use `E:/Godot/Godot_v4.7.2-stable_win64.exe`, workspace cwd, process-local APPDATA `.godot/appdata`, LOCALAPPDATA `.godot/localappdata`, and local log files. Every child was launched with `Start-Process -PassThru -WindowStyle Hidden`, PID reported, and bounded WaitForExit with Stop-Process on timeout. Functional/capture timeout is 20 seconds; measurement process timeout is 35 seconds with an internal 25-second elapsed deadline. All final children exited; no profiling process is left running.

```powershell
$env:APPDATA = Join-Path $PWD '.godot/appdata'
$env:LOCALAPPDATA = Join-Path $PWD '.godot/localappdata'
$godotTask = Start-Process -FilePath 'E:/Godot/Godot_v4.7.2-stable_win64.exe' -ArgumentList '<arguments below>' -WindowStyle Hidden -PassThru
Write-Output "Godot PID $($godotTask.Id)"
if (-not $godotTask.WaitForExit(20000)) {
    Stop-Process -Id $godotTask.Id
    throw 'Godot task timed out'
}
```

| Run | Arguments after executable | PID | Result |
| --- | --- | --- | --- |
| Final wall suite | `--headless --path "E:/AI Projects/Games/Ring Zero" --script tests/presentation/test_wall_view.gd --log-file .godot/t022-wall-tests.log` | 26820 | **84 passed, 0 failed**, exit 0 |
| Final live suite | Same path/headless, `--script tests/presentation/test_live_view.gd --log-file .godot/t022-live-tests.log` | 31720 | **66 passed, 0 failed**, exit 0 |
| Final standalone build suite | Same path/headless, `--script tests/presentation/test_build_view.gd --log-file .godot/t022-build-tests.log` | 27056 | **49 passed, 0 failed**, exit 0 |
| Final rendered captures | `--path "E:/AI Projects/Games/Ring Zero" --script tests/presentation/capture_wall_view.gd --log-file .godot/t022-captures.log` | 34196 | **51 passed, 0 failed**, exit 0 |
| Sealed rendered diagnostic | Same rendered script/path, `--log-file .godot/t022-load-sealed.log -- --sealed` | 3672 | 1027 passed, 0 failed; 120 frames completed |
| Angular rendered diagnostic | Same rendered script/path, `--log-file .godot/t022-load-angular.log -- --angular` | 25740 | 1028 passed, **1 failed**; 120 frames completed but continuous-angular requirement failed |

Earlier functional attempts: PID 3252 hit a target-array access after the old panel click unexpectedly paused the tree and was stopped at its 20-second timeout; PID 3216 failed a bounded diagnostic guard. Correcting the click to the feedback area resolved the fixture pause. PID 30572 then passed 84 wall checks; initial capture PID 35844 passed 51. Final runs above include the live idle-copy correction. The existing root-certificate-store diagnostic appears in this environment; passing runs have no script/runtime errors.

Actual Viewport input and button events cover Wall entry, wedge placement, occupied Flak-slot independence, one deduction, failures with no mutation, GUI isolation/cancel/release, paused building and resumed angular motion. Assertions verify outer-radius arc geometry, HP status, hidden broken-support lines, open-gap detours with no wall damage, 25% sealed wall damage while wedge HP stays intact, real wall break and later-machine redirection, inner collapse with surviving outer walls/Relay, and loss/error disabled controls. Existing live/build suites retain weapon/ring purchase, pause, loss, camera and startup coverage. The rendered suite checks a real MultiMesh instance transform against its angularly displaced snapshot.

## Captures and explicit fixture overrides

All four are actual OpenGL viewport PNGs saved after frame_post_draw at 1280x900. Final images were inspected for neutral edge placement, angular marker locations, readable controls/HP, visible breach, and preservation of outer walls/Relay. No effects, audio, new art style, side walls, selling, repairs or player-facing cheats were added.

- [Open-gap funnel](artifacts/T-022-open-gap-funnel.png): zero scheduled spawns, Flak damage 0, wedge/machine HP 100000, fixture energy 100000, walls on ring 1 except wedge 12. Eight machines begin at radius 192 in wedges 1,2,3,4,8,9,10,11 with DPS 8 and speed 1; 35 actual ticks move them toward the opening without wall damage. Selected wall remains HP 50.
- [Sealed wall damage](artifacts/T-022-sealed-wall-damage.png): same overrides, all 12 ring-1 walls, one machine at wedge 3 / radius 192, DPS 8. After 60 ticks the selected wall has HP 48, showing 2 HP damage at the 25% multiplier; underlying wedge remains intact.
- [Wall breach](artifacts/T-022-wall-breach.png): continues the sealed fixture, with test-only damaged wall HP set to 0.01 before an actual tick destroys it. A later machine is inserted at wedge 4 and advances ten ticks toward the wedge-3 opening. The line disappears and feedback reports the wall break.
- [Wall-bearing ring collapse](artifacts/T-022-wall-ring-collapse.png): zero-spawn profile, fixture funds 100000, purchased ring 2 with Relay at wedge 3 / slot 1. Inner walls at wedges 8–12; outer walls at 3 and 4. Ring-1 wedges 1–6 and ring-2 wedge 7 are pre-broken, ring-1 wedge 7 HP is 0.01. A machine at radius 192 / wedge 7, HP 1000, DPS 6, speed 1 breaks the seventh inward wedge in one tick, producing authentic collapse. Only the two outer wall arcs and outer Relay remain.

## Pre-correction rendered diagnostics (historical)

Both diagnostics retained all **1000 machines/instances**, 11 Flak, speed 1, three-second warmup, 120 full-scene frames and the existing 60 Hz clock/catchup policy. Renderer was OpenGL 3.3 Compatibility on NVIDIA RTX 4080 / driver 610.88, VSync mode 1 enabled, Engine.max_fps 0. No rate reduction, frame skipping, deduplication, stationary substitution, camera culling or production tuning was used.

The sealed fixture uses ring 1 with 12 walls and machines spread over all 12 surface wedge centers. The angular diagnostic fully buys ring 3, walls its outer edge except wedge 12, and places machines at radius 384 (ring 4 / radial 0) in wedges 5–7. This explicitly differs from the independent headless ring-1 angular reference: the longer arc was intended to preserve movement across warmup and sampling. Eleven Flak remain on ring 1 except Relay wedge 6; the angular outer machines are out of their range, so zero weapon hits are expected there.

Initial diagnostics used durable HP 1e12 / DPS 0.01. At that HP, tiny per-tick wall damage falls below floating-point resolution, so measured wall-HP delta was zero; the 50-HP functional fixture separately proves quarter-DPS damage. Astra approved correcting **test-only** measurement health to 1e8 / DPS 1. The final rerun with those values is reported above; the original measurements below remain historical diagnostics. Production profile data is unchanged.

| Measurement (median / nearest-rank p90) | Sealed diagnostic | Mixed angular/contact diagnostic |
| --- | ---: | ---: |
| Full frame interval ms | **50.113 / 52.831** | **136.599 / 158.191** |
| Simulation CPU ms/frame | **43.720 / 46.277** | **129.478 / 149.906** |
| Snapshot/refresh CPU ms/frame | 3.929 / 4.255 | 3.942 / 4.263 |
| Draw submission CPU ms/frame | 0.970 / 1.009 | 1.532 / 1.622 |
| Ticks/frame | 3 / 3 | 8 / 8 |
| Total measured ticks | 370 | 783 |
| Measured simulation seconds | 6.167 | 13.050 |
| Route rebuilds during sample | 0 | 0 |
| Active min/max | 1000 / 1000 | 1000 / 1000 |
| Angular probe frames | 0 / 120 | **59 / 120** |
| Machines changing angle across sample | 0 | 1000 |
| Weapon hit events | 1320 | 0 |
| Walls/wedges broken | 0 / 0 | 0 / 0 |

Statistics use the average of the two central samples for an even median and nearest-rank p90. Timers measure the normal scene's summed simulation.step CPU, sync and draw preparation separately; frame intervals include engine/render/VSync/scheduling and small measurement overhead. No GPU-only attribution is claimed. Cumulative route rebuilds are differenced across sampling. Movement compares polar wedge/fraction values, including representative checks each frame and all 1000 before/after; weapon hits and breaks accumulate across every simulated callback, not merely the last tick of multi-tick frames.

**The angular result is diagnostic only.** Catchup expanded the sampled simulation interval to 13.05 seconds after warmup, exceeding the ring-3 travel window. All 1000 moved angularly during the sample, but the representative machines were still angular on only 59 of 120 frames before reaching contact. The explicit continuity assertion failed; it was not relaxed. This cannot establish full-window funnel performance and is not a stationary-only acceptance claim.

Simulation CPU dominates both historical results while drawing remains about 1–1.5 ms and no routes rebuild during the sample. The independent T-021 headless reference supplied by Astra was sealed 14.654 ms median / 15.312 p90 and angular 17.279 / 17.476 per tick; the rendered catchup behavior was consistent with insufficient remaining frame budget. T-023 subsequently corrected three simulation costs, and both representable-HP fixtures were rerun as recorded at the top. Performance improved but remains unaccepted; the strict angular criterion still fails. Presentation source and final results are frozen.

Kevin's wall/funneling prototype review follows Astra's validation. This delivery makes no final balance, art, usability, or 1000-cap FPS acceptance claim.

## Post-T-025 unchanged-fixture rerun

Astra accepted the T-025 owned-combat-copy correction after independent 1423 checks and exact comparison of 246 digests. Public pure API semantics were reported unchanged. This presentation lane made **no source or fixture changes** for the rerun. Both rendered tests ran sequentially in the exclusive Godot window with the existing HP 1e8 / DPS 1, all 1000 machines/instances, speed 1, 11 Flak, three-second warmup, 120 full-scene frames and unchanged 60 Hz clock/VSync settings.

| T-025 measurement (median / nearest-rank p90) | Sealed contact | Continuous angular routing |
| --- | ---: | ---: |
| Full frame interval ms | **16.650 / 16.863** | **49.120 / 49.666** |
| Simulation CPU ms/frame | **9.806 / 10.183** | **42.204 / 42.761** |
| Snapshot/refresh CPU ms/frame | 3.809 / 3.926 | 3.826 / 3.889 |
| Draw submission CPU ms/frame | 0.978 / 1.013 | 1.503 / 1.544 |
| Ticks/frame | 1 / 1 | 3 / 3 |
| Total measured ticks | 120 | 336 |
| Measured simulation seconds | 2.000 | 5.600 |
| Route rebuilds during sample | 0 | 0 |
| Active min/max | 1000 / 1000 | 1000 / 1000 |
| Angular probe frames | 0 / 120 | **120 / 120** |
| Machines changing angle across sample | 0 | **1000** |
| Weapon hit events | 440 | 0 |
| Walls/wedges broken | 0 / 0 | 0 / 0 |
| Wedge HP delta | 0 | 0 |
| Wall HP delta | -499.999523 | 0 |

The unchanged strict angular condition **passes**: all representative frame probes remain angular across the entire 120-frame window, all 1000 machines change angular position, no wall damage occurs, and no machine count is reduced. This is now valid full-window angular evidence for the documented ring-3 fixture. It does not erase the earlier failed diagnostics or establish final 1000-cap FPS. Sealed contact is near a 60 Hz interval with p90 slightly above 16.667 ms; angular frame time remains around 49 ms and simulation dominates at roughly three ticks per frame. No measured route rebuilds explain that cost. Angular weapons remain outside range in this approved fixture, hence zero weapon hit events.

| T-025 run | PID | Log | Result |
| --- | ---: | --- | --- |
| Rendered sealed | 17020 | `.godot/t022-load-sealed-t025.log` | **1027 passed, 0 failed**, exit 0 |
| Rendered angular | 39444 | `.godot/t022-load-angular-t025.log` | **1029 passed, 0 failed**, exit 0 |

Commands are the same tracked `Start-Process -PassThru -WindowStyle Hidden` invocations described above, with the respective `--sealed` / `--angular` flag and T-025 log name. Both finished within the 35-second process timeout and 25-second internal deadline. Renderer remains OpenGL 3.3 Compatibility / RTX 4080 / driver 610.88, VSync 1, max_fps 0. Only the existing root-certificate diagnostic appears; no runtime errors or assertion failures occurred.

No new consumer compatibility concern appeared, so focused wall/live suites were not repeated in this measurement-only window; the last functional counts remain 84 wall, 66 live, 49 build passes. Reviewed behavior captures remain unchanged. All Godot children have exited, results/report are frozen and the exclusive measurement window is released.
