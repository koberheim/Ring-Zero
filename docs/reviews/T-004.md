# T-004 delivery — procedural grid inspection

Date: 2026-09-06. Owner: Luna. Ready for Astra review; Kevin's human readability review remains pending.

Implemented the default three-band inspection scene, neutral generated core/annular polygons, persistent selection status, camera-corrected left selection, middle drag pan, and reversible 10% wheel zoom. Geometry consumes PolarGrid bounds and polar conversion; excluded angular endpoints use the adjacent wedge's included start. Rebuilding clears the derived polygon arrays, replaces the grid, clears selection, and fits the viewport. Boundary strokes stay one screen pixel wide; selection is three pixels. No production art or gameplay was added.

## Verification

Pinned engine: `E:/Godot/Godot_v4.7.2-stable_win64.exe`, reported `4.7.2.stable.official.ed1daf0bf`. All commands ran from `E:/AI Projects/Games/Ring Zero`, with process environment set first:

```powershell
$env:APPDATA = Join-Path $PWD '.godot/appdata'
$env:LOCALAPPDATA = Join-Path $PWD '.godot/localappdata'
```

Commands and actual results:

```powershell
& 'E:/Godot/Godot_v4.7.2-stable_win64.exe' --headless --path . --log-file .godot/t004-import.log --editor --import
# Exit 0; import succeeded.
& 'E:/Godot/Godot_v4.7.2-stable_win64.exe' --headless --path . --log-file .godot/t004-core.log --script res://tests/core/test_polar_grid.gd
# Exit 0; 1,206 checks, 0 failures.
& 'E:/Godot/Godot_v4.7.2-stable_win64.exe' --headless --path . --log-file .godot/t004-presentation.log --script res://tests/presentation/test_grid_inspection.gd
# Exit 0; 6,668 checks passed, 0 failed.
$captureProcess = Start-Process -FilePath 'E:/Godot/Godot_v4.7.2-stable_win64.exe' -ArgumentList '--path "E:/AI Projects/Games/Ring Zero" --log-file "E:/AI Projects/Games/Ring Zero/.godot/t004-capture.log" --script res://tests/presentation/test_grid_inspection.gd -- --capture' -WindowStyle Hidden -PassThru -Wait
# Exit 0; 6,674 checks passed, 0 failed (including six capture checks).
& 'E:/Godot/Godot_v4.7.2-stable_win64.exe' --headless --path . --log-file .godot/t004-launch.log --quit-after 2
# Exit 0.
```

The graphical run followed the boundary stroke correction and repeated the complete presentation suite. It used OpenGL 3.3 compatibility rendering on NVIDIA GeForce RTX 4080. Root certificate-store warnings are environmental. Core tests explicitly bracket their expected invalid-input diagnostics.

Presentation assertions cover generated counts and replacement for 3 → 12 → 3 → 0 bands, polygon sampling, all tested polygon vertices on API radii and cell centers inside polygons, 100-band construction (1,201 polygons), core/cardinal/outside selection, status, 1.1 wheel multiplication and inverse pair, unchanged zoom center, screen-delta drag divided by zoom, release stopping drag, no drag selection, selection after pan/zoom and resize, resize preserving camera position, rebuild fit/reset, north-up rotation, and zero/nonfinite zoom safeguards. Mouse tests use `Viewport.push_input` with `InputEventMouseButton` and `InputEventMouseMotion` through the scene's actual `_input` handler. Failures exit nonzero.

## Rendered evidence

All captures are actual 1280 × 900 engine viewport PNGs saved after `RenderingServer.frame_post_draw`:

- [Three bands](artifacts/T-004-three-bands.png): centered core plus three bands, no selection.
- [Twelve bands](artifacts/T-004-twelve-bands.png): centered fitted twelve-band rebuild, no leftover polygons.
- [Panned and zoomed selection](artifacts/T-004-panned-selected.png): ring 2 / wedge 3 selected after middle drag and two wheel-up events.

Luna inspected all three PNGs: circles remain round, fitted views contain the entire structure, twelve-band boundaries are visible, and the selected east cell has a distinct neutral outline. Navigation intentionally allows outer geometry to leave the viewport, visible in the panned capture.

Review follow-up: after the twelve-band rebuild, actual click events at ring 12 / wedge 12 and ring 12 / wedge 1 now assert both selected identifiers and displayed status. The affected headless and graphical presentation suites were rerun successfully (6,668 and 6,674 checks respectively); graphical capture refreshed the same three PNGs. Both runs include the screen-consistent boundary correction.

Limitations: automated injected input is not a physical mouse usability test; screenshots establish rendering evidence, not Kevin's human readability approval. Arc sampling is finite rendering approximation and no performance claim is made for very large ring counts.
