# Feature completion contract — 2026-09-08

Kevin authorizes continuing through feature completion, autonomous decisions except art, and recording those choices for later confirmation. This supersedes earlier phase approval stops. Original design specification stays untouched. Final game art, final animation/audio production and publishing remain excluded. UI direction is explicitly gritty industrial sci-fi with legible genre typography. Luna checks in every 15 minutes, earlier only for a blocker or handoff. No repeated status polling of Luna.

Clarification after audit: D-104/T-055 already explicitly require one-action whole-ring purchase. Q and the Ring Plate button each execute one validated purchase immediately, with no relay-slot or world-placement follow-up. This overrides the generic key-only-select rule for Q alone. Ignore echoed/held repetitions. Other building keys select tools; Y selects reclaim then requires one ring click. G selects relay rebuild then one ring click. Update controls/tutorial and old intentional input assertions accordingly.

Transfer audit resolution: after its one-ring hop, a private navigation corridor along the inner boundary of the skipped ring supports normal finite-speed angular travel, respecting inward walls/debris and forbidding outward travel through intact material. Other machines never gain access. Cache by skipped ring/topology where practical. A fully sealed targetless interior permits a bounded wait until topology changes. Assembler collapse growth retains the existing final personally broken wedge plus destroyed occupant/wall count; other erased wedges do not add growth. Reject nonfinite growth/damage and stack overflow atomically.

Sapper fallback remains deliberately relay-focused: at a destroyed relay it waits, threatening any rebuild, until topology changes or it is killed. It does not silently become a wedge attacker. Reference text must explain this; retain as a provisional gameplay review item.

Profile clarification: retain at most128 settlement receipts and an optional `settled_run_amounts` map for conflicting retained reward detection; older allocated IDs remain non-creditable through monotonic next_run_id, capped below JSON's exact-integer limit. A crash before a completed result is durably saved abandons that run; no mid-run recovery or guaranteed unsaved reward is claimed.

## Shared completion interfaces

Root owns contracts and decision records. Terra owns gameplay and content data. Sol owns persistence and tooling. Luna owns presentation, project display settings, scenes and presentation tests. Do not edit another lane's files. Coordinate Godot execution windows. Existing tests remain isolated from persistent real profiles.

### T-065 — Terra: complete rules, run choices and accounting

First finish T-062 audit and correct reproduced Phase 9 bugs with discriminating tests, preserving approved D-104 through D-110. Keep skipped-band Transfer exception bounded, and handle Debris/terrain eligibility without global false rejection. All kinds must participate in real configured runs. Preserve old LiveSimulation.create defaults for regression fixtures.

Add `src/gameplay/run_rules.gd` class_name RunRules and `data/balance/campaign.json` for editable campaign definitions. Public pure API:
- `catalogue() -> Dictionary`: dictionaries `doctrines`, `loadouts`, `mutators`, `unlocks`, `upgrades`, `reference`; stable string IDs, human labels/descriptions, modifiers/costs. Return copies.
- `default_choices() -> Dictionary`: `{doctrine:"conservator", loadout:"balanced", mutators:[]}`.
- `prepare(base:BalanceProfile, choices:Dictionary, progress:Dictionary, practice:bool=false) -> Dictionary`: `{ok, errors:PackedStringArray, profile:BalanceProfile, build_modes:Array[StringName], abilities:Array[StringName], reward_multiplier:float, choices:Dictionary}`. Validate IDs, duplicate mutators, unlocks and level caps. Apply modifiers once to a deep copy; return validated BalanceProfile. Practice grants catalogue access only and cannot earn rewards. Structural actions expand/wall/armor/repair_node/repair/reclaim/rebuild_relay always available.
- `create_run(base, choices, progress, practice=false, ring_limit=9223372036854775807) -> Dictionary`: same prepare fields plus simulation with all seven enemy kinds enabled. Existing direct live test scenes can retain full testing access; normal application uses this API.
- `reward(summary:Dictionary, multiplier:float=1.0) -> Dictionary`: `{ok, errors, amount:int, breakdown:Dictionary}`. Strict finite/nonnegative/integer validation, no reward for abandoned/error/practice runs.

Use spec's provisional doctrine names: Conservator (repair/reclaim cost x0.75, armor HP bonus x1.25, weapon damage x0.9); Prospector (kill credit x1.5 rounded to integer, wedge HP x0.8); Interdictor (terrain cost x0.75, Screen speed multiplier x0.8, weapon damage x0.9). Costs round up, integer HP/cost constraints validated. No economic ability credit. Three starting loadouts: balanced existing Flak; bulwark Flak plus one starting Wall wedge12 with starting energy reduced by its cost; battery starter Mass Driver in place of Flak, starting energy reduced by positive cost difference. All retain ring1 and 200 baseline energy before modifiers/costs. Do not silently convert production start to clean slate.

Three optional stackable mutators: dense_swarm spawn rate x1.5/reward x1.25; fragile_core core HP x0.75/reward x1.2; rapid_elites all elite/boss first/interval timings x0.75/reward x1.25. Multipliers multiply once; choices fixed for a run. Keep 1000 editable cap and test stat ramp; do not claim final swarm pacing.

Starter access: all doctrines and balanced loadout; Flak/Mass Driver/Point Defense, all structure, Debris and EMP Burst. Purchasable unlocks: EMP Node25, Tractor25, Occlusion40, Lance50, Flare50, bulwark25, battery25. Three capped upgrades: core HP +5%/level, wedge HP +5%/level, starting energy +10/level; max3, next costs25/50/75. No uncapped growth. All in campaign data, validated.

Add authoritative run accounting to LiveSimulation: total kills (weapons+abilities), highest owned ring, relay rebuild count; exactly once after committed commands/ticks only. `run_summary(outcome:String="defeat") -> Dictionary` returns `{outcome, elapsed_seconds, kills, highest_ring, relay_rebuilds}` copied, no presentation-derived rewards. Supported outcomes defeat/abandoned/error/practice. Reward floor(seconds/60)*2 + floor(kills/25) + 5*max(0,highest_ring-1), plus challenges survive300s:+10, kills250:+10, rebuild1:+5; apply selected multiplier then floor once. Challenges reward per completed run, explicitly visible, no hidden unlock criteria.

Own gameplay/data/new gameplay tests and docs/reviews/T-065.md. Do not modify presentation or persistence. Done: all choices/unlocks materially enforced in prepared run, input immutable, invalid combinations rejected, numerical caps/costs tested, all enemies enabled through create_run, rewards/accounting include ability kills once but no energy, long mixed encounter and original suites remain correct. Send API freeze before Luna wires screens.

### T-066 — Sol: durable profile and private tooling

After baseline and root runtime handoff, add `src/core/profile_store.gd` class_name ProfileStore. Persistent path `user://profile.json` injectable for isolated tests. API instance `load_profile(path:String="user://profile.json") -> Dictionary`, `save_profile(profile:Dictionary,path=...) -> Dictionary`; results `{ok, profile, errors:PackedStringArray, recovered:bool}`. Store version1 profile `{version:1, currency:0, unlocks:[], upgrades:{}, settings:{fullscreen:false, ui_scale:1.0, reduced_motion:false, effects:true}, tutorial_completed:false, next_run_id:1, pending_run_id:"", settled_run_ids:[]}`. `begin_run(profile)->Dictionary` returns copied profile and unique run_id; abandoned pending ID yields no reward. `settle_run(profile,run_id,amount:int)->Dictionary` credits only matching pending ID once, clears pending, records settled IDs; duplicate is successful no-op. `purchase(profile, id, cost:int, upgrade_level:int=-1)->Dictionary`: strict ID/cost/level validation, deep-copy result and debit once; unlocked duplicate rejected, upgrade must be exactly previous+1 <=3. Catalogue eligibility/cost comes from RunRules in controller, never UI editable numbers. Invalid amounts/overflow fail atomically. Keep bounded settled history with monotonic next_run_id protecting older duplicate IDs.

Atomic temp write + close + rename, recoverable last-valid backup, strict schema/ranges and unknown-new-version rejection. Never overwrite unreadable/incompatible existing profile without explicit recovery path; recovery from backup is reported. Missing creates defaults. Avoid filesystem mutation on validation error; rollback preserves valid primary on write failure. Fixtures must use unique workspace/temp path and never real user://profile.json. Return actionable errors.

Record local engine/template availability. Prepare one-command standalone private Windows export if templates already available; do not publish. If unavailable, record precise dependency; feature work continues. Add reproducible source snapshot/inventory tooling excluding .godot, user profiles and large captures, and keep runner existing baseline checks meaningful. Own core/store tests/scripts/export config and docs/reviews/T-066.md; project.godot display/main scene belongs Luna. No mid-run save/resume. Done: independent disk reload, exactly-once settlement, corrupt/backup/newversion/write failure behavior, isolated fixtures and native suite pass.

### T-067 — Luna: application, 16:9 industrial UI and learning

After T-064 plan/root token review, own src/presentation, scenes, project.godot, presentation tests/captures, docs/CONTROLS.md and docs/reviews/T-067.md. Check-in interval15min; no subdelegation. Build theme and layout immediately; wire shared APIs after owner freeze. Do not duplicate rules or persistence. Main application scene becomes `scenes/application.tscn`, with script `src/presentation/application.gd`; existing direct live_view/art_preview test scenes remain runnable. Main application hosts a LiveView or art-preview child using prepared run. Inject prepared profile/simulation without starting a duplicate run or changing simulation API defaults.

16:9 base1440x810, minimum1280x720, support1920x1080 and other windows via preserved aspect/letterbox (no world stretching). Native screenshots and actual viewport-coordinate input at1280x720/1920x1080, plus resize and fullscreen. Keep controls compact, legible, style consistent across HUD/start/results/settings/tutorial. Existing hotkeys and sticky placement remain. Enforce RunRules build/ability access at UI dispatch/button paths and action time. Show lock reasons, not dead controls.

Screens: Start (new run, doctrine/loadout/mutator selection with actual descriptions/effects, progression shop, tutorial, settings, quit); game HUD (energy/core HP/radius/time/enemy count, active tool, compact catalogue, solar cooldowns, usable radial status and selected ring power/relay); pause menu (resume, restart, return to start with explicit abandon action and no rewards, settings); defeat results (authoritative summary and reward breakdown, successful saved reward, retry or return). Settlement must save successfully before showing credited balance; on failure allow retry save and clear error, no duplicate credit. Begin run/save lifecycle controller owns pending IDs; practice/tutorial cannot grant campaign rewards.

Progression shop uses catalogue costs/levels and ProfileStore mutations; persist settings immediately and apply fullscreen/UI scale(1.0/1.15/1.3), reduced motion and optional functional shot effects. Keep text and focus visible; no color-only critical state. Escape precedence consistent across screens. Keypress alone never buys a building/casts. Rebuild Relay shortcut choose G if free, labels/help match current relay-independent one-click ring purchases. Start game is not a debug funding path.

Replayable tutorial: isolated practice session with accessible full catalogue, no persistent reward, visible Tutorial label; step card and replay/exit paths. Explain and exercise selection/build+kill income, expansion, wall funneling, seven-break collapse, relay brownout/rebuild, repair/reclaim, enemy counters, solar targeting, minimap. Can use explicit tutorial-only damage/quiet setup per step, never production hidden grants. Advancement uses performed actions/observed state for interactive steps; reference explanations have explicit Next. Finishing saves tutorial_completed only. Reference includes all seven enemy kinds and their counters.

T056 functional feedback bundled: graybox enemy distinctions/labels for all kinds, elite warning and relay-loss/restoration text, weapon hit traces/EMP pulse/Lance line from committed hits; short bounded pooled/capped transient effects only, no combat delays/change. Respect effects/reduced-motion settings. No final sprites/animations or new game art direction. UI may use code-native styled panels and existing legally redistributable fonts; never copy movie assets/logos. Document chosen fonts/license and design tokens.

Done: actual UI input fresh start→configure→play→defeat→reward→shop→relaunch→new run; abandoned/practice no rewards; locked tools cannot be selected by hotkey; tutorial completion; settings persist; all screens readable and all controls functional in final16:9 captures, no hidden blockers; original direct test scenes pass adjusted intentional layout assertions. Root visually reviews. No claims of final art or human playtest acceptance.

### T-068 — integrated final review

Root sequences final native suites after owner freeze, source snapshot, mixed encounter/scale samples, lifecycle/persistence/campaign scenario checks and native UI capture review. Record actual failures and fix feature blockers before stopping. A private candidate is feature complete only with every named mechanic and between-run path wired and tests passing. Human balancing, final asset/animation/audio production and final crowd-cap approval are later reviews; do not call those completed. Decisions above remain provisional under delegated authority, listed for Kevin's confirmation.
