# T-025 - Avoid copying private combat staging twice

Owner Terra. Own src/gameplay/weapon_rules.gd, src/gameplay/live_simulation.gd, tests/gameplay/test_weapon_rules.gd, tests/gameplay/test_live_simulation.gd, docs/reviews/T-025.md. No core/presentation/balance changes or delegation. Read owned files and this brief only.

Measured T-023 WeaponRules cost is about4.3ms sealed/5.8ms angular per1000 tick. LiveSimulation already constructs private staged target dictionaries; WeaponRules currently duplicates those dictionaries and their positions again before changing HP. Public pure callers still need independent results. This task removes only the redundant private copy, then measures the real gain.

Keep public WeaponRules.step(state,profile,targets,cooldowns,delta) signatures, full validation, pure input isolation, result shape, ordering, cadence, targeting tolerances and errors unchanged. Add private-convention WeaponRules._step_owned with the same arguments/result shape. It consumes an exclusively owned array of target dictionaries: it may update their HP even before a later failure, but never mutates any PolarPosition or nested extra data. Positions may be borrowed read-only. State/profile/cooldowns remain inputs only. Validate all target records and cooldowns before combat work as before. Reuse one common internal implementation with an explicit ownership/copy policy rather than two combat algorithms.

LiveSimulation may call _step_owned only for its private staged target records. On failure discard that scratch and preserve all pool payloads, state, core, cooldowns, events, navigation and waypoints exactly. Admission rollback remains unchanged. Public targets_snapshot and public WeaponRules.step still return independent cloned positions and target dictionaries. Neither internal API may publish scratch via a public result before commit. Internal sharing is safe because motion replaces positions rather than changing fields; WeaponRules reads positions and only changes scalar HP in exclusive records. Explicitly document these lifetime rules.

First preserve a frozen before measurement and behavior reference, then implement this one change. Do not alter target selection, add reduced-rate targeting, change crowd size, skip clock time, alter health, weaken validation or add a generic trusted-input bypass. If needed later, report geometry/selection costs separately for another bounded correction; no spatial index or core motion rewrite in this task.

Tests: all prior219 weapon checks and current293 live checks remain. Compare public/owned results on copied fixtures with multiple weapons, kills, ties, arcs/range boundaries, partial cooldowns, collapsed structures and failures. Verify public result mutation cannot touch input nested data or positions; owned call never changes position fields; late failure after partial damage leaves live pool unchanged. Preserve exact T-023 baseline state/event/waypoint digests where applicable.

Measure before/after1000 sealed and active-angular full ticks, with3warmups/120samples including normal scheduled volleys. Use existing representableHP1e8/DPS1 fixtures; report all moving/attacking counts, hits, rebuilds and phase median/p90. Separate correctness digest runs from timing-only diagnostics: prior outside-timed deep-copy/hash work showed timing sensitivity. Retain anomalous results and do not infer a cause. Public pure-call performance must also be checked for material regression. Coordinate exclusive Godot timing window through Astra; pinned4.7.2, workspace caches, hidden tracked bounded processes.

Done when a measured gain preserves behavior/ownership tests, source is frozen and Astra reviews before Luna reruns the unchanged full rendered fixture. If no safe gain, revert and report; do not broaden implementation without a new brief.
