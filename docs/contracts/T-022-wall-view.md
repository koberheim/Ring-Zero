# T-022 - Present and operate the reviewed wall/funneling test

Owner: Luna. Start only when assigned after T-021 acceptance. Own src/presentation/live_view.gd, src/presentation/build_view.gd, tests/presentation/test_live_view.gd, tests/presentation/capture_live_view.gd, tests/presentation/test_wall_view.gd, tests/presentation/capture_wall_view.gd, docs/reviews/T-022.md, docs/reviews/artifacts/T-022-*.png and generated metadata. No gameplay/core/balance edits or delegation. Read owned files and this brief only.

Approved D-065: choose Wall in Structure tab and click an intact owned wedge to place one wall on its curved OUTER edge, separate from building slots. Cost5/HP50 from profile. Show a neutral line on that edge; reuse existing grayscale/font/line widths. Broken supporting wedge disables blocking; collapse removes walls. D-066/D-067/D-068: normal enemies detour to exposed surfaces whenever reachable; only a sealed reachable region permits wall HP damage at25% of normal DPS. When a wall breaks, route toward the opening. No new shake/audio/effects/final styling, side walls, selling, repair or cheats.

Integrate through LiveSimulation.place_wall(ring,wedge)->{ok,state,errors}. It already persists state, validates support/duplicates/funds/ended rules and invalidates routes. Never call WallRules.place directly from the view. Existing live APIs and clock behavior remain. Add wall_button with profile economy.wall_cost text. Base standalone build view should retain previous supported modes; add minimal virtual mode/placement hooks as needed rather than adding unsupported wall behavior to that scene. Live wall mode uses selected_cell; slot index does not affect placement. Invalid placement spends nothing and retains mode; success clears mode. Right-cancel, GUI isolation, middle-release and menu pause remain. Core loss/runtime error disable wall button and placement too.

Wall records: optional state.rings[r].wedges[w].wall={hp:positive,max_hp:positive}. No record means absent/destroyed. Active support requires wedge.hp>0 and noncollapsed ring. Draw only active wall edge lines so broken support does not look blocked; use outer radius CORE_RADIUS+r*RING_WIDTH and the existing wedge bearing convention. Render geometry is derived, not competing game state. Simple neutral selected-wedge status may report Wall HP for inspection using existing label treatment. Do not draw a side wall or place walls on unowned/broken cells. One wall remains independent of Flak/Relay occupants.

Live events now add walls_broken:Array[Vector2i]. Plain wall-break feedback uses the existing label, with core/ring-collapse messages taking priority. Machines are still rendered from snapshots with all-target batching, but now have angular fractions that change while routing. Do not snap markers to wedge centers or assume only radial motion. Preserve3-screen-pixel circles under pan/zoom and all1000 instances.

Fixture correction required by T-021: old inner-collapse fixtures inserted an enemy at band2/wedge7 while that ring2 wedge was intact. Normal movement now rejects such embedded sources. Make ring2/wedge7 a pre-broken approach gap before placing the attacker at radius192, and continue checking that other ring2 wedges and Relay survive ring1 collapse. Update only those invalid setups in test_live_view/capture_live_view; retain their real controls/pause/loss assertions. Historical T-018 PNGs remain historical; new T-022 captures describe current fixtures.

Known fixture APIs: BalanceProfile.load_json("res://data/balance/testing.json")->profile; profile.with_overrides(nested)->{ok,profile,errors}; LiveSimulation.create(profile,3)->{ok,simulation,errors}. Existing test fixture() helper creates a zero-spawn profile and simulation. Relevant keys: economy.wall_cost,health.wall_hp,health.wedge_base_hp,health.standard_machine_hp,standard_machine.damage_per_second,standard_machine.wall_damage_multiplier=.25,pressure.spawn_per_second,max_active_machines=1000. Simulation state/pool may be mutated ONLY in tests. pool.spawn({position:PolarPosition,hp,dps field damage_per_second,speed_ring_widths_per_second})->ID; payload_for(id)->live dict; active_count()/active_ids()/release unchanged. PolarPosition(ring,wedge,radial_fraction,angular_fraction). Ring2/radial0 is ring1 outer surface192. Replace view.simulation/profile then sync_simulation() after fixture construction. All actual movement/combat should then run through scene ticks, not fabricated event arrays.

Verify actual Wall button and wedge clicks: one cost/no slot consumption; duplicate/insufficient/unowned/broken rejection without mutation; menu freeze and resumed movement; panel clickthrough/right-cancel; existing weapon/ring purchases; loss disables wall controls. Integration fixtures must show one unwalled gap causes actual angular detours with no wall damage, full sealing allows25% wall damage while underlying wedge stays intact, wall destruction removes line and redirects later machines, and ring collapse clears wall lines while surviving outer walls remain. Assert continuously changing polar angular fractions are reflected in marker transforms. Reuse meaningful gameplay APIs rather than copying route rules into UI tests.

Capture actual1280x900 PNGs of an open-gap funnel, sealed wall damage/breach, and wall-bearing ring collapse with surviving outer structures. Clearly label any fixture funds/HP overrides in report. Run final live/build/wall focused suites; inspect all new images. Keep project main_scene live_view.tscn. No normal-UI test controls or grants.

Measure rendered1000-machine fixture(s) with actual angular routing and sealed contact using the approved cap, existing batch markers and60Hz clock:3-second warmup/120frames, VSync unchanged. Record full-frame/simulation/sync/draw timings, tick and route-rebuild counts, active counts, movement/attack events and fixture limitations. No stationary-only claim for funnel performance; no smaller count, skipped updates, deduplication or tuning/cadence changes to improve results. Return a bottleneck for review if necessary before undertaking expensive changes.

Pinned Godot4.7.2; workspace cache/log paths; tracked hidden process with finite timeout. Astra reviews source, reruns focused tests and inspects images. Then Kevin reviews the wall/funneling prototype; no final balance/art/cap acceptance implied.
