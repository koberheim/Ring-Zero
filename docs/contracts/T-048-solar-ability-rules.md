# T-048 — Solar ability rules and live commands

Owner Terra. Own new src/gameplay/ability_rules.gd, live_simulation.gd, balance_profile.gd, data/balance/testing.json, focused gameplay tests, docs/balance-data.md and docs/reviews/T-048.md. No presentation/core changes or delegation. T-043 accepted and frozen before this task. Implement only the two approved abilities, not terrain in this task.

Authority: D-026 no energy from ability kills; D-097 clicked bearing/point targeting, targetable enemies only, valid casts start cooldown, invalid/cancelled casts do not. Spec: Focused Flare is a wide directed discharge, high damage/long cooldown; EMP Burst is an area stun with low damage/short cooldown. Both cost only cooldown, never energy/charges. D-033 delegates first-pass numeric tuning, all values validated and documented.

Contract: AbilityRules.cast(profile: BalanceProfile, targets: Array[Dictionary], cooldowns: Dictionary, kind: StringName, aim: PolarPosition) -> Dictionary. Pure input ownership. Kinds focused_flare and emp_burst. Return ok, errors, targets, cooldowns, hits, kill_ids, energy_awarded (always zero), consistent with WeaponRules result conventions. Cooldowns keyed by these StringNames, missing means ready, entries finite nonnegative. Flare uses the bearing from core to aim (zero-length aim invalid), affecting targetable living targets in its outward angular sector within configured range from core. EMP uses a circle around aim's world position. Valid empty-area casts consume cooldown. Validate positions/statuses before calculations and fail atomically on malformed/nonfinite computed values. Preserve deterministic target order. Underground enemies remain immune. EMP stun refreshes with max(existing,new), using existing duration semantics.

Add editable focused_flare fields damage, cooldown_seconds, range_ring_widths, arc_degrees; emp_burst fields damage, cooldown_seconds, radius_ring_widths, stun_seconds. Reuse existing schema validation types. Choose documented provisional values appropriate to current 10-HP normals under D-033; do not adjust existing balance. No new aiming-distance restriction for EMP.

LiveSimulation: add ability_cooldowns: Dictionary = {}; abilities_snapshot() -> Dictionary returns an independent cooldown copy; cast_ability(kind: StringName, aim: PolarPosition) -> Dictionary validates run active, stages through pure helper, then commits HP/stun and releases each killed ID exactly once. It returns its own event payload, using existing events shape with kill_ids/hits and zero energy, independent of last_events from the fixed tick. Do not repeat cast kills on next step. UI will count command-event kills once separately. Never mutate positions, navigation, weapon cooldowns, structural state, elapsed time or energy as part of cast. Failure leaves every live field/payload unchanged. If necessary validate full live targets with existing helpers, not unsafe coercion.

Each successful fixed step decrements ability cooldowns by FIXED_STEP, clamped with existing cooldown roundoff tolerance, inside stage/commit so failed ticks do not consume them. Ended/no-op ticks do not advance them. New simulations start ready. Cancellation is UI-only and invokes no cast.

Done: pure and live tests cover sector/radius boundaries, inward/opposite exclusion, multi-kill with zero energy and no duplicate next-tick kill, underground immunity/surfaced eligibility, stun refresh, ready/cooldown/empty-area/invalid/ended casts, input snapshot isolation, atomic failed casts and failed ticks, independent ability/weapon cooldowns, editable values and malformed values. Run focused affected suites using pinned hidden bounded Godot and workspace caches. Coordinate the exclusive test window with Astra; do not re-record performance baselines. Report exact results and any rule ambiguity before expanding scope.
