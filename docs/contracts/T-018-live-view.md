# T-018 - Present and operate the reviewed live test

Owner: Luna. Do not start until Astra accepts T-017 and assigns this brief. D-061 placeholder presentation must be approved. Own src/presentation/live_view.gd, scenes/live_view.tscn, src/presentation/build_view.gd, project.godot, tests/presentation/test_live_view.gd, tests/presentation/capture_live_view.gd, docs/reviews/T-018.md, docs/reviews/artifacts/T-018-*.png and generated metadata. No gameplay/core/balance edits, no delegation. Read owned files and this brief only; use supplied APIs rather than inventing simulation behavior.

Extend accepted build view with a LiveSimulation. Minimal refactor of build_view allowed: extract purchase/quote calls and ownership/display helpers into virtual methods so existing build view keeps its standalone behavior while live subclass delegates to simulation. Keep prior purchase controls, slot geometry, north-up pan, wheel10%, click-through prevention, right-cancel over panels, and middle-release handling. Existing foundation/build scenes remain runnable. Set main_scene to live_view once tests pass.

After base _ready/profile load, LiveSimulation.create(profile,ring_limit)->{ok,simulation,errors}; if failure, disable gameplay clearly. Exposed simulation, clock:FixedStepClock, last_error:String for tests. LiveSimulation public read-only state,core_hp,elapsed_seconds,ended,cooldowns,pool,last_events; targets_snapshot()->Array[Dictionary] independently copied {id,position:PolarPosition,hp,damage_per_second,speed_ring_widths_per_second}. outer_owned_ring()->int. quote_expansion(wedge,slot)->existing quote format; purchase_ring(wedge,slot),place_weapon(ring,wedge,slot,kind)->{ok,state,errors}. Successful methods already persist simulation.state; do not overwrite it with UI copies. state in view follows simulation.state. All live purchases must use simulation methods so occupied-band rejection and ended checks cannot be bypassed.

FixedStepClock.new(60.0,callback); advance(frame_delta)->count; paused bool, elapsed_seconds(),tick_count. Each callback calls simulation.step(1/60)->{ok,errors,events}. Scene _process calls clock.advance(delta), setting clock.paused to SceneTree.paused or simulation.ended or a recorded runtime error. Root/UI process ALWAYS inherited; menu must still freeze gameplay with no resumed catchup. On unexpected step failure stop the clock and display error instead of continuing corrupt state. Preserve base menu's prior SceneTree pause restoration. Do not step while paused just because root processes ALWAYS.

Refresh rendered target snapshot after actual simulated ticks, not independently each draw. Convert polar positions with a sufficiently large temporary PolarGrid for target bands; selection/build grid remains the approved slice size. No scene-node-per-machine requirement; choose straightforward batched drawing consistent with approved markers, and measure. No authoritative Cartesian state in view. Do not cull simulation based on camera. Keep selection/panning outside owned area unchanged.

Approved live behavior: additive new-spawn stats only for testing, radial inward normals for this initial wall-free Phase 2 loop, 1000 editable max active, skip arrivals at capacity, auto-attack/target buildings, damage/kill energy, 7-break ring collapse destroys that ring only, core loss ends gameplay, occupied proposed ring prevents expansion. Walls/flow-field funneling, Tunneler, power, retaking, abilities remain later tasks. Never add cheats to normal UI.

Ring state retains contiguous historical IDs. record.get("collapsed",false) marks destroyed rings; all wedges hp0/occupants empty/relay empty. Do not show collapsed cells as owned intact cells or offer build slots there. Partial broken wedges hp0 also show no active build slot. A surviving outer ring remains visible after inner collapse. Keep empty geometry outlines for orientation; do not renumber rings. Next purchase candidate still historical rings.size()+1; never offer automatic retaking.

D-061 proposed visuals (use only if approved): existing neutral grayscale, small filled circle machine markers, plain core HP and active-enemy count, darker broken wedges, immediate removal of collapsed occupants; no effects/audio/shake/final art. Use existing labels/fonts/styles; avoid panel overlap. Core death shows plain 'Core lost' and disables building, while camera/menu may remain usable. No new restart behavior is needed for this review; Godot rerun starts a fresh test.

Events {spawned_ids,kill_ids,broken_wedges:Array[Vector2i],collapsed_rings:Array[int],hits,energy_awarded,core_lost}. Show plain immediate feedback for wedge break, ring collapse, core loss, and occupied-ring purchase failure; avoid overwriting these every draw with generic instructions. The minimal fields are test feedback, not approval of the future full ring-status HUD D-024.

Verify actual scene integration: default startup; visible scheduled arrivals; movement and automatic kill with energy bank once; normal placement through simulation; occupied-ring rejection/spend isolation; pause freezes elapsed/targets/HP/cooldowns/energy and resumes without catchup; GUI no clickthrough; right-cancel/middle-release still work; broken/collapsed geometry/slots/occupants update; surviving outer ring stays; core loss stops purchases/ticks; positions render beyond selection-grid ring count. Use isolated fixture changes only in tests/capture, never player cheats. Rerun existing test_build_view.gd (find exact existing name within owned test folder if needed), plus focused live tests. Avoid rewriting accepted gameplay tests or values to make visual checks pass.

Capture actual 1280x900 rendered PNGs for natural early combat, a fixture showing inner collapse with surviving outer structure, and core loss. Review all captures; no placeholder claim from headless rendering. Record fixture overrides clearly. For rendered load measurement, use a fixture of1000 durable machines, low/zero DPS, enough armed buildings to exercise targeting (11Flak on ring1 exceptrelay6); three-second warmup, then120 frames of full scene operation. Record frame interval and simulation CPU median/p90 separately, renderer/VSync conditions and limitations. Do not claim final FPS or lower the approved test cap; report any performance problem for Astra. Each launched Godot process must be tracked with Start-Process -PassThru -WindowStyle Hidden and bounded completion, avoiding orphaned test processes on parser failure.

Pinned Godot E:/Godot/Godot_v4.7.2-stable_win64.exe, workspace APPDATA/LOCALAPPDATA and logs. Return focused/existing tests, screenshot paths, measured results, and source summary for Astra review. This is a user review point after Astra verifies the visible live loop, not final slice acceptance.

API clarification: FixedStepClock calls its callback with one delta_seconds:float argument (exact fixed step). Accept that argument and pass it to simulation.step. The unchanged hands-off profile loses the core at about19.97seconds; natural early-combat capture must precede collapse at18.68seconds. Do not change tuning to extend captures; controlled later-state captures are explicit fixtures.
