# T-026 - Tunneler data and pure arrival/destination rules

Owner Terra. Own src/gameplay/tunneler_rules.gd, src/gameplay/balance_profile.gd, data/balance/testing.json, tests/gameplay/test_tunneler_rules.gd, tests/gameplay/test_balance_profile.gd, docs/balance-data.md, docs/reviews/T-026.md. No live simulation, WeaponRules, core or presentation edits; no delegation. Read ownedfiles and brief only. Runtime follows separate accepted handoff.

Approved D-018/D-075/D-076: bypass one intact ring; arrive already underground at its perimeter; untargetable underground; warning/burrow2seconds then vulnerable. On scheduled bearing, find outermost intact wedge, then next intact wedge inward. Broken/collapsed gaps do not count. If fewer than2, arrival is ineligible/skipped without backlog. Destination is middle of inner wedge, locked at admission. Structure changes later do not relocate it; if targetgone, emerge in gap and follow normal routes. Runtime will damage intact destination wedge until it breaks, then use normal movement. D-077 warningappearance pending: no visual/runtime integration here.

Add required profile fields health.tunneler_hp=20 (positive) and tunneler={first_arrival_seconds:60,arrival_interval_seconds:30,burrow_seconds:2}, all finitepositive. Same normal damage/surfacedspeed/killreward and additive spawn-time growth. Existing max_active_machines is shared, no new cap. Existing profile schema_version1 retained as internal pre-save schema; strictmissing/extra/wrongtype checks remain. All new values editable via current profile factories/overrides, documenttestingonly.

TunnelerRules extends RefCounted. static arrivals_between(profile,start_seconds,end_seconds)->{ok,first_sequence:int,count:int,errors:PackedStringArray}. Interval(start,end], nonnegativefinite start<=end. Sequence1 at first_arrival, next at first+(sequence-1)*interval. Same MachineSpawnRules count-roundoff tolerance1e-12 arrival units and signedint overflow rejection. No deferred backlog state. Sequence determines bearing12,1..11 repeating, including ineligible/skipped sequence positions. Validate profile and numericinputs; errors contain no partialcount.

static spawn_descriptor(profile,state:Dictionary,sequence:int)->{ok:bool,eligible:bool,spawn:Dictionary or null,errors:PackedStringArray}. Validate RingPurchaseRules state/profile and positive sequence. Search historicalrings descending on scheduledwedge, exclude collapsed or hp0. If fewerthan2, oktrue/eligiblefalse/spawnnull/emptyerrors. Malformedinput okfalse/eligiblefalse/spawnnull/errors. Scheduledtime and computedstats must be finite, reject integer/timeoverflow, no fallbackvalues.

Eligible spawn fields EXACT: sequence:int,elapsed_seconds:float,kind:&tunneler,phase:&burrowing,targetable:false,position:PolarPosition,start_position:PolarPosition,destination:PolarPosition,surface_cell:Vector2i,burrow_elapsed_seconds:0.0,burrow_duration_seconds:profilevalue,hp:scaledhealth.tunneler_hp,damage_per_second:normal spawn-scaledDPS,speed_ring_widths_per_second:normal speed. Positions: start=(outer+1,wedge,0,.5), destination=(inner,wedge,.5,.5), surface_cell=(inner,wedge). position and start_position are independently owned clones; destination independent. Data returned cannotmutateinputstate/profile. No worldpositions stored.

Existing helper contracts: RingPurchaseRules.validate_state(state,profile)->PackedStringArray; BalanceProfile.from_dict(profile.snapshot()) validatesfullprofile. MachineSpawnRules.stats_at(profile,elapsed)->{ok,stats:{hp,damage_per_second,speed_ring_widths_per_second},errors}; growthfactor=1+floor(elapsed/60)*pressure.stat_increase_per_minute. Calculate own20baseHP with samefactor; normalhelper may supplyDPS/speed. PolarPosition(ring,wedge,radial_fraction,angular_fraction). State rings are contiguoushistoricalkeys; each .wedges[1..12] has hp; collapsedbooloptional. No core0 target. CORE_RADIUS96,RING_WIDTH96 only if numericrepresentability checks needthem. No movement implementation inthis task.

Tests: exact60/90/... boundaries, intervalpartition equivalence,12-bearing cycle, invalidprofiles/fields/overflow; defaultone-ring ineligible;two/three-ring picks, holes/collapsedrings skipped, missinginner skip, immutablelockedreturnedpositions across subsequentstatechanges, independentpositionreferences,20baseHP with additiveperminute spawn scaling,DPS/speedinheritance and overrides. Profile tests and building/weapon/spawn consumers remainpassing. Do not update existing normal behavior or digestbaselines.

Use pinnedGodot4.7.2/workspacecaches, hidden trackedprocesses finitewait. Exclusive measurementwindow throughAstra (focusedtests no fullcrowd benchmark needed for this pure sparsehelper). Reportchangedfiles/tests/limitations; freezeafterdelivery. Done whenAstraaccepts purecontract before assigningruntime. D-077 doesnotblockthisfoundation.
