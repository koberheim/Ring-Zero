# T-051 — Playable terrain and solar controls

Owner Luna, after T-049 art-preview freeze. Solar controls may begin against accepted T-048 while terrain rules are verified; terrain controls wait for T-050 review. Own existing presentation build/live files and art-preview integration as necessary, focused presentation tests/captures, docs/reviews/T-051.md. No core/gameplay edits or delegation. Functional controls use existing typography/theme; art treatment remains the separate approved comparison. No new combat effects/audio.

Enable Terrain tab with Debris Field, Tractor Lane, Occlusion Screen buttons and profile-derived prices. Select kind then existing slot placement invokes simulation.place_terrain(ring,wedge,slot,kind,direction). Tractor direction is an explicit clockwise/counterclockwise control, mapped +1/-1. Display chosen direction before placement. Clear rejection text for slot/support/energy/one-lane limit/unreachable sealed perimeter. Show actual terrain occupant labels/symbols; allow inspecting selected occupant and direction. Broken support disables effect visually using existing broken-state treatment. Preserve UI click interception, cancel, camera, world selection and all prior tabs.

Add Focused Flare and EMP Burst buttons with remaining cooldown. Choosing one enters a separate ability-target mode; left world click converts to authoritative PolarPosition and invokes cast_ability(kind,aim), right click cancels with no command. Flare indicator shows bearing/sector from core; EMP indicator shows circle about the pointer. Use data range/arc/radius for these plain outline functional indicators. Do not cast from clicks on UI, menus or outside valid conversion. Cooling-down buttons give readable feedback. Invalid targeting keeps cooldown untouched and displays reason; successful cast clears mode and updates targets immediately. Casting does not pause the game.

Command returns events independent of fixed tick: increment run_kill_count once from cast events.kill_ids, then sync_simulation; never count cast kills through last_events again. Energy must remain unchanged even after ability multi-kill. Retry clears ability input state and shows ready cooldowns. Menu blocks casts; switching build/ability mode cancels previous selection. Both normal main and inherited art preview receive these actual gameplay controls.

Done: actual input through all three terrain purchases and both solar casts, direction/wrap selection, atomic denied placement, cooldown/cancel/invalid/UI-hit cases, underground immunity, ability kill results/no energy, repair-reactivated terrain behavior, mode switching/menu/retry and an outer-ring example. Capture normal and art-preview controls without overlapping labels/buttons or obscuring meaningful board targets. Verify target outlines agree with gameplay boundaries within rendering precision. Use pinned hidden bounded Godot, coordinate exclusive window, and report results before Phase 8 review.
