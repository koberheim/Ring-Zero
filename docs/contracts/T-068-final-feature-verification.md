# T-068 — Final feature verification

Astra coordinates after T-065/T-066/T-067 source and focused-test freezes. User authorizes continuing until feature complete, excluding final assets/animations. No premature completion based on pure helpers or a test count. No additional permission gate is needed for the following local checks.

1. Native clean import and every discovered correctness suite using pinned4.7.2 engine. Record actual child exits, timeout status and error scan. Preserve negative-test diagnostic distinction. No digest regeneration to hide gameplay changes; review any mismatch before changing historical baseline.
2. Rules: all seven enemy kinds enabled in configured normal runs; finite-speed post-hop Transfer respects walls/debris; Sapper breaks relay and rebuild restores downstream output; Assembler attribution and overflow guards; mixed encounter includes effects, terrain, topology changes and shared cap. Doctrines/loadouts/mutators materially affect validated profiles and run state. Restricted tools remain locked even through command APIs. Abilities never award energy; authoritative totals count their kills once.
3. Persistence: new profile, start durable runID, defeat reward, purchase, disk reload and next run. Retry settlement once and conflicting/old IDs, failed writes, stale writer, corrupt backup recovery and future version rejection. Isolated paths only; do not reset Kevin's profile. Unsaved completion after a crash is explicitly outside the no-resume policy.
4. Presentation: actual keyboard/pointer inputs through final application viewport at1280x720,1440x810,1920x1080 and a non16:9 window. Start/configure/play/menu/restart/abandon/results/shop/settings/relaunch/tutorial/replay/quit paths all work. Check saved UI scale1.0/1.15/1.3 and focus/readability; letterbox borders do not cast/build. Q direct ringpurchase once, G targeted relayrebuild, all other hotkeys/locks/cancel/repeat semantics. Pending settlement failure does not display nonexistent credit. Tutorial is clearly isolated and no-reward.
5. Native rendered screenshots: start/configuration, HUDexpanded/collapsed, powerfailure/relayrebuild, each distinctiveeliteplaceholder, shop/results/settings/tutorial atminimumsize and1080p. Root views final images at native resolution; resolve clipping, invisible buttons and unreadable contrast rather than merely asserting rectangles.
6. Private export from frozen source using actual installed pinnedMonoengine/templates if verified; launch exe in isolated fresh profile context outsideeditor, exercise start/play/menu/quit and reload. Record exe/pck and font licenses, exactversion and localprerequisites. Never publish/distribute. Refresh export after final fixes, not before.
7. Scope-correct performance samples: representative ring/weapon/enemy/terrain configurations, including1000actors; fullsimulation tick costs and renderedwholeframes separately. Include topology changes, effects on/off and appHUD; record cap/skippedadmissions and any inability to meet16.67ms. No artificial caplowering, actorremoval, timewarp or droppedticks to claimsuccess. Existing performance limitation stays explicit if unresolved; do not present quiet100actorVSync captures as capacityproof.
8. Final source inventory/snapshot, current logs/decisions, reproducible launch/testcommands, artifacts and a detailed userbriefing. Mark implementation/correctness separately from human balance/art/experience acceptance. List D111-D117 confirmation points and any evidence-backed remaininglimitations.

Boundaries: Sol owns core/persistence/tooling verification, Terra rules/mixedencounters, Luna presentation/captures with15-minutecheckins, rootread-only review/logsintegration. Serialize engine runs, confirm ownershiphandoff before launch, leave unrelatededitorprocesses untouched. Any discovered bug returns to itsowner withconcretereproduction, followed by relevantcheck and finalaggregatewhen needed.
