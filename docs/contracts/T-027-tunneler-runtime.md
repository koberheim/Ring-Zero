# T-027 - Integrate the approved Tunneler lifecycle

Owner Terra. Start only after T-026 acceptance and D-079 resolution. Own src/gameplay/live_simulation.gd, src/gameplay/tunneler_rules.gd (runtime pure helpers if needed), src/gameplay/weapon_rules.gd, tests/gameplay/test_live_simulation.gd, tests/gameplay/test_tunneler_rules.gd, tests/gameplay/test_weapon_rules.gd, docs/reviews/T-027.md. No core/presentation/profile edits or delegation. Read ownedfiles and thisbrief. Ask Astra for missing contracts.

D-018/D-075/D-076 approved, exactdescriptor below. Two-second burrow arrives alreadyunderground, bypasses first intact wedge, emerges at center of next inner intact wedge. Admission requires both. Targetlocked; structurechange cannotrelocate. Underground no damage and untargetable. Surfaced damages its intact destination wedge frominside at normalDPS, so outeredgewalls do not intercept that attack. After that wedge breaks, follows existing normal open-cell movement/wall rules. If destination alreadybroken/collapsed at emergence, directly uses normal movement fromthat now-open cell. No retarget deeper whileunderground, no directcore emergence, no new cap or cadence.

Keep LiveSimulation.create(profile,limit) backwardcompatible and add optional third include_tunnelers:bool=false, stored as tunnelers_enabled. This is temporary integration control: current mainview must remain wall-only until D-077 is approved and Luna explicitly enables it. No UI toggle/cheat. Runtimefixtures use true. Normal-only state/events/snapshots must preserve T-023 baseline digests exactly. New balance fields exist afterT-026 regardless offlag.

Admission: merge normal and eligibleTunneler due times chronologically before movement. Exactties admit the eligible Tunneler before the normal arrival (D-079 Option A approved). Preserve lifetime-ID order of actualadmissions. All share poolcapacity. IneligibleTunnelers spend no slot; bothstreams skip blockedarrivals withoutbacklog. Keep existing skipped_arrivals as normalcount; add skipped_tunneler_arrivals with integeroverflowguard, committed onlyafter successful tick. Preserve admit-before-movement/fixedtick semantics; availabletime fornewentity=clamp(nextelapsed-scheduledelapsed,0,dt). Do not enumerate unbounded duecounts: aftercapacity fills countremainingasskipped; ineligiblebearingcycle checks are bounded by12 using frozen admission-state eligibility. Descriptorvalidation/errors remain atomic.

T-026 TunnelerRules APIs: arrivals_between(profile,start,end)->{ok,first_sequence,count,errors}, sequence1 at60,interval30; spawn_descriptor(profile,state,sequence)->{ok,eligible,spawn,errors}. Eligible spawn fields: sequence,elapsed_seconds,kind:&tunneler,phase:&burrowing,targetable:false,position,start_position,destination:PolarPosition,surface_cell:Vector2i,burrow_elapsed_seconds:0,burrow_duration_seconds:2,hp,damage_per_second,speed_ring_widths_per_second. Start=(outer+1,w,0,.5),destination=(inner,w,.5,.5). Hp20basewithsameadditivegrowth; DPS/surfacedspeedfromnormalprofile. No descriptor retains state/profile references.

Tunneler phases: &burrowing -> &surface_attack -> &roaming. kindremains&tunneler. targetable derived consistently fromphase(falseonlyburrowing). Stage all mutable scalarphase/timing fields with targetrecords; borrowedPolarPositions remain read-only. Public targets_snapshot must independentlyclone ALL exposedPolarPositions includingstart/destination. Normaltargetrecords retain oldshape. Commit metadata onlyafter allfalliblerules succeed; release cleanup/lifetime semanticsunchanged. No new eventkeys needed: existing spawned_ids/kill_ids plus snapshotphase/countdown are sufficient forLuna. Normal last_events stayexactlyunchanged.

Duringburrow derive constantphysicalspeed=(startradialradius-destinationradius)/duration. Use accepted PolarMotion.advance on supplied same-bearingsegment. Elapsedtime cannotexceedduration; reachingduration usesexactdestination andtargetabletrue. Spend leftoverdt after emergence onunderfootattack ornormalmovement, without extra wholetick delay. Availabletime0 maynotadvance. Never damage whileunderground. Onunderfootwedgedeath use existing shared _attack_surface destruction/collapse routine; sameattacker stopsforthattick, latersortedIDs see updatedtopology. Ifalreadygone, normalmovement uses currentstagednavigation. Currentintact-cell rejection remains fornormalenemies; onlyvalidatedburrowing or ownsurface_attackTunneler mayoccupy intendedintactcell. Once roaming, normalvalidationapplies.

Validate lifecyclemetadata, finitepositive duration, boundedelapsed, readonlystart/destinationshapeandbearing, surface_cell correspondence, phase/targetableconsistency. Reject malformedstate before mutation. Do not weaken standard target validation. Preserve stagedcache/waypoint rollback and newlyadmittedentityrollback including consumedIDs. Coreloss stops subsequent attacks andweapons asbefore, and freezesremainingburrows.

WeaponRules: reserve optional targetable:bool(defaulttrue). Validate exactbool whenpresent forALL records, including skippedones. Bothpublicpure andprivateowned paths ignore false targets for selection/damage/killcredit, but returnthem unchanged andstillvalidateHP/position. Public snapshots/results remainisolated; owned path nevermutatesposition/nesteddata. Preserve normal targetorder/ties/range/arc/cooldown semantics. A burrower thatsurfaces duringmovement canbetargeted inthattick's existing weaponphase. No separate weaponalgorithm.

Existing corecontracts: EntityPool contains(id)->bool,active_ids()->sortedPackedInt64Array,payload_for(id)->liveDictionary,release(id)->bool. PolarMotion.advance(current,target,speed,dt)->{ok,position,time_used,reached,errors}, nonmutating, tolerancecloseendpoint maytime0. PolarGrid CORE_RADIUS96,RING_WIDTH96. Fixedstep1/60. Existing normalroutehelpers andcollapse logic remain authoritative.

Verify full lifecycle usingdurable/zero-normal-spawn fixtures and realfixedticks:60sec eligibility/skip and90secbearing progression; holes/collapsedrings; mixedstream ordering andD-079lastslot; no backlog; partialtickadmission;2secemergence/countdown; no undergroundhits/damage; surfacedhiteligibility andsinglekillreward; innerwall remainsprotectedfromoutside but Tunneler damages its supportingwedge frominside; wedgebreak thenroaming/corepressure; targetdestroyed midburrow; expansionkeepsdestination; pause(no step),coreloss; malformedphase/targetable; publicmetadataisolation; failedcombat afterphaseprogress/admission preservesoldpayloads/phase/destination/cache. Existing weapon/live/navigation/normalspawn/build/wallconsumers pass; no normaldigestbaseline rewrites. Explicit fixtures enableTunneler flag. Defaultvisibleview remains disabled.

Run pinned4.7.2/workspacecaches with hidden trackedboundedprocesses; coordinateexclusive measurementwindow. Check unchanged normal1000timing for materialregression, no newcrowd targetclaim. Deliverstablecode/report with countsandlimits; Astra reviewsbeforeLuna integration, whichstillrequiresD-077.

D-077 Option A is now approved; keep defaultview unchanged until Luna integrates the actual warning/triangle renderer after this gameplay review.
