# T-043 - Correct verified Phase 7/8 contract regressions

Owner Terra. Approved scope is corrective, not new mechanics. Own src/gameplay/live_simulation.gd, weapon_rules.gd, and their gameplay tests; docs/reviews/T-043.md. Additional approved audit correction: balance_profile.gd, data/balance/testing.json, test_balance_profile.gd and docs/balance-data.md for the unchanged assimilation defaults described below. Other gameplay files only for a reproduced validation correction after notifying Astra. Do not change core, presentation, default tuning values, or recorded baseline. No delegation.

1. Clone assimilation_stacks for public targets_snapshot records; private staged borrowing is allowed only while readonly. Validate mutable status fields before copying/coercing/iterating: stun_remaining optional for legacy supplied targets, defaults zero; present value must be finite numeric and nonnegative (bool is not numeric). assimilation_stacks optional defaults empty; present must be array of finite nonnegative numeric expiry times. Reject invalid data through existing structured failure paths, not runtime exceptions. Preserve public WeaponRules purity and live rollback, including admissions, navigation, cooldowns and existing payload references.
2. D-088 Option A says fixed-radius EMP pulse on its own ring, every living targetable eligible machine. Add own-ring filter; no truncation by max_targets for this kind. D-089 Option A says every machine in its wedge column outward to max range. Exclude inward targets (use radial position relative to firing origin within existing tolerance) and do not truncate this kind by max_targets. Keep distance range and exact wedge membership, fixed firing order, power allocation, damage/cadence and all other weapons unchanged. Legacy max_targets data may remain validated for schema compatibility but explain why unused for these two approved all-target weapons.
3. Stun expiration must absorb only existing cooldown roundoff, not create an extra full frozen tick after the approved duration. Validate and test exact 90-tick/1.5-second expiration. Preserve current skip-movement-and-attack tick semantics; do not invent partial-tick resumption policy.

Reproduce before fix, then regression-test: public snapshot append/clear/entry edits; bad statuses even with no ready weapon and malformed admitted/existing targets; failed tick after scratch mutation; EMP excludes adjacent ring and includes >configured-cap eligible targets; Lance excludes inward same-column and cross-wedge targets and includes >cap outward targets; Point Defense remains exact-cell; generic weapon cadence/ties remain unchanged.

4. D-094 approved: block repair of a broken (zero-HP) wedge while any living machine occupies that exact authoritative ring/wedge cell. Check in both quote and mutation through one consistent live path; no spending/state change on rejection. Still-intact damaged wedges remain repairable, including a valid surfaced Tunneler attacking inside them. Ignore dead enemies. Preserve pure RingPurchaseRules contract (it has no entity pool); the live wrapper owns occupancy. Existing expansion/reclaim rules remain unchanged. Add a regression reproducing successful repair then invalid tick before correction, and afterward rejection with uninterrupted ticks, no charge, then successful repair once the threat leaves/dies.

D-021 wedge-break grant wording versus retained occupants is not authority to delete/grant new behavior. Do not change that interaction within T-043. Record any further issues rather than expanding scope.

5. Root source review found assimilation defaults hardcoded as .05,10,15 despite the standing editable-combat-data requirement. Move those unchanged defaults into validated assimilation fields (bonus_per_stack,max_stacks,stack_seconds), and read them in both grant/prune and damage multiplier paths. Add meaningful override/validation checks; document fields as first-pass testing values. This restores the data contract, not a new balance decision. A reproduced nonfinite armor-HP addition may receive a fail-before-spend guard and regression in BuildingRules/test_building_rules after reporting it; do not retune armor.

Use pinned Godot4.7.2 with workspace caches, hidden tracked bounded processes. You own the exclusive verification window initially; notify Astra before/after tests and release it at freeze. Run focused gameplay consumers; unchanged cost baseline only once if justified, never re-record to conceal failures. Report changed behavior, tests and remaining limitations. Astra reviews before accepting; Luna is wiring only presentation in parallel without tests until your window releases.
