# T-047 — Playable expansion beyond three rings

Owner Luna. Close the named D-087/A2 carryover. Own src/presentation/grid_inspection.gd, build_view.gd, live_view.gd, scenes/live_view.tscn, focused presentation tests/capture and docs/reviews/T-047.md. No gameplay/core edits or delegation. Preserve approved neutral presentation and all T-045 fixes.

Add an exported live-view switch `unbounded_expansion: bool = false`; enable it in the main live_view.tscn. Existing bounded fixtures keep ring_limit behavior. In this mode create the simulation with the largest supported positive integer limit, but never allocate a grid at that limit. ring_limit continues to describe the initial preview extent (three by default). This is an internal representational bound, not a chosen game cap.

Synchronize displayed geometry to max(initial preview extent, historical owned ring count + 1) only when this count changes. Include the next purchasable preview ring. Preserve camera position, zoom, north-up orientation, current selection and input context when extending geometry; do not use the current reset-everything rebuild without a preservation path. Reset deliberately on a new run as existing lifecycle requires. Expand world conversion/hit testing, build slots, wall geometry, marker support and HUD focus to the same displayed grid. Inspect all ring_limit uses, including sync_simulation's outer extent. Never allocate proportional to the maximum integer.

Done: actual main scene can buy through ring 12 with fixture funding and quiet pressure, select/build on those rings, focus from HUD, pan/zoom, repair/reclaim, and retry without errors or stale geometry. Normal startup receives no extra money. Verify rejected purchases do not grow owned state; preview growth does not move camera; bounded existing tests remain valid. Capture a readable large-ring view with existing styling. Run meaningful focused tests, not another full suite until integration review. Coordinate exclusive Godot window with Astra; report evidence, limits and source freeze.
