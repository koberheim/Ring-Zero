# T-019 - Approved outer-edge wall placement rules

Owner: Terra. No delegation. Own src/gameplay/wall_rules.gd, src/gameplay/ring_purchase_rules.gd, tests/gameplay/test_wall_rules.gd, docs/reviews/T-019.md and generated .uid files. Read owned files and this brief only. Existing ring purchase validator is yours from T-017. No live_simulation, core, presentation or balance edits.

D-065 Option A approved: one wall on the curved outer edge of any intact owned wedge, separate from normal building slots. Cost comes from economy.wall_cost (currently5), HP from health.wall_hp (currently50). Wedge break disables wall blocking; whole-ring collapse removes the wall. No side edges, selling, refund, repair, relocation or implicit wall price scaling. D-066 is now Kevin's Option C: normal machines can damage walls at reduced damage only when the entire perimeter is walled. Exact reduction/closure semantics are pending, and no combat/routing behavior belongs in this task.

Represent the outer-edge wall as optional wedge.wall Dictionary {hp:finite positive,max_hp:finite positive}, with hp<=max_hp. Absence means no wall. ZeroHP walls are destroyed and should not be retained; future combat will erase them. Reject unknown wall keys, malformed types/numbers, nonpositive/max overflow. Broken wedge hp0 may retain a positive wall record but it is inactive; collapsed ring must have no wall record on any wedge. Add this optional-field validation to RingPurchaseRules.validate_state without weakening existing occupied-slot/relay/tombstone rules. Existing states with no wall field remain valid.

WallRules extends RefCounted:
- static place(state:Dictionary,profile:BalanceProfile,ring:int,wedge:int)->Dictionary {ok:bool,state:Dictionary or null,errors:PackedStringArray}. Validate state/profile through existing RingPurchaseRules.validate_state. Reject nonexistent/nonpositive rings, wedge outside1..12, collapsed/broken supporting wedge, any existing wall, insufficient energy, invalid computed profile wall HP/cost. Successful result is a deep-copied state with wedge.wall={hp:profileHP,max_hp:profileHP}, energy decremented once. Preserve all occupants/slots/relay and profile, no input mutation. Use profile.value("economy.wall_cost") and profile.value("health.wall_hp"). Normal profile validation already enforces nonnegative integer cost and positive HP. No fixed literals/defaults.
- static is_blocking(state:Dictionary,ring:int,wedge:int)->bool. Lightweight read-only query for a maintained validated state: true only if ring/wedge exists, ring is not collapsed, wedge.hp>0, wall exists and wall.hp>0. Return false for missing ring/wedge/wall; this method is not a substitute for whole-state validation.

Known existing build state: {energy:number,rings:Dictionary keyed contiguous historical ring ints}. Each ring {wedges:Dictionary keyed1..12,relay:{wedge,slot}, optional collapsed:bool}; each wedge {hp,max_hp,slot_count,occupants:Dictionary keyed slots->{kind:StringName}}. Collapsed rings keep IDs and dimensions but all hp0, occupants empty, relay empty. All relevant public rules already call RingPurchaseRules.validate_state. BuildingRules.create_default_testing_state(profile)->{ok,state,errors}; RingPurchaseRules.purchase_next_ring(state,profile,relay_wedge,relay_slot,ring_limit)->same; BalanceProfile.load_json(path)->{ok,profile,errors}, with_overrides(nested)->same, snapshot/value unchanged.

Done: verify5-energy/50HP data-driven placement, tuned values and free cost override, one charge, no slot usage or occupant loss (including starter Flak/Relay wedges), duplicate placement rejection, valid inner/outer owned-ring placement, broken/collapsed/unowned/invalid IDs rejection, insufficient funds atomicity, returned state isolation, inactive wall on broken support, malformed wall records and walls on collapsed tombstones rejected. Existing building/weapon/live consumers must still pass; Astra reruns them after reviewing your source and focused tests. No UI button or visible wall is added before the subsequent live rules handoff.

Pinned Godot E:/Godot/Godot_v4.7.2-stable_win64.exe, workspace APPDATA/LOCALAPPDATA/log paths. Track process PID via Start-Process -PassThru -WindowStyle Hidden with bounded WaitForExit. Report exact checks and files in docs/reviews/T-019.md. Stop for Astra review; no pending combat multiplier chosen.
