# T-044 - Reliable native Windows correctness runner

Owner Sol. Own scripts/run_tests.ps1 (new), scripts/run_tests.sh only to document/delegate supported invocation without breaking existing Bash use, scripts/README.md (new), docs/reviews/T-044.md. No core/gameplay/presentation/test-suite/baseline edits or delegation.

Current Bash runner lacks clean-import preparation and prerequisite/nonempty discovery checks. Add a native PowerShell entry point for this Windows workspace. Resolve project root from script location; default pinned E:/Godot/Godot_v4.7.2-stable_win64.exe but allow explicit executable override. Validate executable/version, set process-local APPDATA/LOCALAPPDATA to project .godot caches, create log directory. Discover test_*.gd under tests explicitly, reject discovery error or zero suites. Import/global-class discovery with headless editor --import --quit, bounded tracked process; abort on failed import. Run suites sequentially with hidden tracked processes and configurable finite timeout; terminate only the process started by the runner. Aggregate nonzero exits/timeouts; never report zero suites as success. Paths with spaces must work. Retain stdout/stderr/logs and per-suite summaries. Avoid destructive cleanup or changing user global environment.

Document accurate current scope: test_*.gd excludes standalone capture/benchmark files, but some correctness suites contain internal timing blocks. Do not claim perfect timing separation or valid benchmark results from this runner. Performance runs remain explicit and exclusive; no optimization or snapshot baseline changes in this task.

Verify missing executable and empty-discovery safeguards without deleting/renaming production tests (isolated temporary project fixture if needed). Verify real import+all suites once only after Astra grants test window and other sources freeze. Unit tests mirroring script are not required. Deliver concise invocation, evidence and limits. D032 editor-only acceptance stands; this is not export/persistence work.
